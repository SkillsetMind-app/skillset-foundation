# Skillset — Backlog de Melhoria Priorizado (FE + BE)

> **Data:** 2026-06-06
> **Método:** Auditoria interna (FASE 1, 9 dimensões) + benchmark competitivo (FASE 2 — Skool + Cakto) consolidados num backlog único e pontuado.
> **No-Invention (Article IV):** toda alegação técnica é rastreada a `arquivo:linha`. Itens marcados ✓ foram confirmados por leitura direta do código nesta sessão; os demais vêm dos finders de auditoria (rotulados como tal — honestidade epistêmica).
> **Repo canônico:** `C:/Users/nicae/aiox-core/projects/skillset-foundation` (NUNCA a cópia do OneDrive).
> **Ground truth desta sessão:** `lint` exit 0 (limpo) · `vitest run` → **22 arquivos / 92 testes passando**.
> **Atualização 2026-06-06 (FASE 2 aprofundada):** itens competitivos C1–C12 refinados com evidência **primária de vídeo** (assisti o produto Skool real) + **exploração ao vivo da Cakto na conta real** (logado, criei um produto de teste do zero, abri cada aba do editor, o checkout builder, o sistema de afiliados ponta-a-ponta e a tabela de taxas do produtor) + **capability map de código** (`file:line`). Detalhe completo em `docs/benchmarks/skool-vs-skillset-2026-06-06.md`.

---

## Como ler a pontuação

Cada item tem **Impacto (1-5)** e **Esforço (1-5)**. Para a ordenação ser sensata (alto impacto + baixo esforço no topo), defino:

```
Facilidade = 6 − Esforço
Score      = Impacto × Facilidade        (faixa 1–25, maior = faça primeiro)
```

Assim um item Impacto 5 / Esforço 1 = **25** (topo), e um Impacto 3 / Esforço 4 = **6** (fundo). As colunas Impacto e Esforço ficam visíveis para transparência total.

Severidade (da auditoria) é independente do score: mede **gravidade técnica** (alta/média/baixa), enquanto o score mede **prioridade de execução** (valor ÷ custo).

---

## FASE 0 — Estado atual (recap de 5 pontos)

1. **Pagamentos LIVE e robustos.** 28 Cloud Functions, Stripe Connect completo (webhook, refunds com reversão proporcional de transfer, cron de release, billing de assinatura). Split verificado em teste: $100 USD → professor $88.80 / plataforma $8.00.
2. **Backend > fachada.** O motor é forte; os gaps reais são de **verdade (copy), acabamento, acessibilidade e camada de conversão/engajamento** — não de arquitetura quebrada.
3. **Discrepância de payout ✅ RESOLVIDA (2026-06-06, T1).** Código vivo = **30 dias** (`payment-rules.ts:12`); a UI já estava em 30 (`payoutClearDays=30`); só os docs históricos diziam 7/10 — agora reconciliados. Single source of truth estabelecido. Sem mais risco de diligence neste ponto.
4. **Stubs de professor alcançáveis pelo menu** (`integrations`, `co-productions`, `team`, `coupons`) renderizam "coming soon" — investidor clicando cai em tela vazia.
5. **Correção: community feed e events JÁ EXISTEM** (`course-community-feed.tsx`, `learn-events-hub.tsx`, `teacher-event-studio.tsx`). O doc Cakto antigo os listava como gap — falso. Gaps competitivos reais encolhem para 3 peças cirúrgicas (gamificação, afiliados, discovery).

---

## ✅ Callout RESOLVIDO: a discrepância de payout (Q1) — reconciliado em 30 dias (2026-06-06)

> **RESOLVIDO (T1).** Decisão do fundador: payout = **30 dias** (canônico, já vivo). Correção importante: a UI **já estava em 30** (data-driven de `payoutClearDays=30`) — a alegação da auditoria de que "a copy de UI diz 7 dias" estava **desatualizada**. O que carregava número velho eram só os **docs/decisões históricos**, agora anotados como SUPERSEDED ou corrigidos para 30. Reembolso segue 7.

Histórico da discrepância (o que foi reconciliado):

| Fonte | Número | Localização |
|-------|--------|-------------|
| **Código canônico (vivo, deployado)** | **30 dias** | `functions/src/payment-rules.ts:12` ✓ |
| Docs antigos (HANDOFF/DECISIONS/roadmap) | "D+10" | `docs/plans/2026-05-31-...md:37` (desatualizado) |
| Copy de UI/marketing | **já em 30** ✓ (a alegação "7 dias" era stale) | data-driven de `payoutClearDays=30` (9 páginas); os "7 dias" remanescentes são a janela de *reembolso* (corretos) |

O refund window é **7 dias** (`payment-rules.ts:27`). Payout ≥ refund (nunca liberar payout enquanto a cobrança ainda é reembolsável) — **30 satisfaz isso com folga**. ✅ **Decisão do fundador (Q1, 2026-06-06): manter 30 + copy honesta.** Executado: comentário em `index.ts:2987`, `DECISIONS.md` (D3/D16→SUPERSEDED + nova D21), roadmap, demo-readiness, `HANDOFF.md`, `TEST_RESULTS.md` e benchmark Cakto reconciliados para 30. Verificado por grep: zero referência de *payout-timing* fora de 30 remanescente.

> **Prova externa (Cakto, capturado ao vivo 2026-06-06):** um PSP maduro **não esconde** payout-timing — ele publica uma **tabela por método de pagamento** (Pix D+1 · Boleto D+2 · Cartão D+15 · wallets D+30). Isso confirma que três números conflitantes (código 30 / docs 10 / UI 7) é exatamente o tipo de inconsistência que diligence pega. Reconciliar e publicar UMA verdade é higiene básica de produto de pagamento — por isso T1 fica no topo.

---

## Backlog completo (pontuado, agrupado por tema)

### 🔴 Backend — correção de fluxo de dinheiro

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| B1 | **Webhook idempotency "claim-before-commit"** — `markStripeEventProcessed` grava o doc ANTES do handler; falha transitória → 500 → Stripe re-tenta → doc já existe → short-circuit → handler nunca re-roda → enrollment/payout perdidos. Fix: deletar o claim no catch, ou two-phase com status (`claimed`→`done`). | `index.ts:2854-2861` + `3685-3695` ✓ | **alta** | 5 | 2 | **20** |
| B2 | **Overwrite fora de ordem** — `checkout.session.expired`/`payment_failed` chamam `markOrderStatus` incondicional; se chegarem após um pedido já `paid`, sobrescrevem o estado pago. Fix: só transicionar de estados não-terminais. | `index.ts:2878-2883` | **alta** | 4 | 2 | **16** |
| B3 | **Guard de compra duplicada não-transacional** — `enrollmentRef.get()` é leitura fora de transação; dois checkouts concorrentes (duplo-clique/duas abas) passam ambos → cobrança dupla. Fix: transação ou idempotência por `userId__courseId` em pedido in-flight. | `index.ts:1345-1356` ✓ | **alta** | 4 | 3 | **12** |

### 🟠 Verdade / Copy (diligence)

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| T1 | **Reconciliar payout (30/10/7)** — escolher o número (Q1) e alinhar TODA a copy + docs ao código. | `payment-rules.ts:12` + 5 arquivos de copy | **alta** | 5 | 1 | **25** |
| T2 | **Stubs de professor no nav** — decidir: esconder do menu na demo, ou rotular claramente "roadmap Q3 + me avise". | `app/teach/{integrations,co-productions,team,coupons}/page.tsx` | **média** | 4 | 1 | **20** |
| T3 | **`/promise` promete o não-shipado** (affiliate, analytics, quizzes, drip, custom domains) — marcar "em breve" para não virar promessa falsa. | `app/promise/page.tsx:17` | **média** | 3 | 1 | **15** |

### 🟡 Acessibilidade / WCAG

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| A1 | **Tokens de dark theme nunca trocam** (accent/success/warning/danger) — CTA primário vermelho falha AA no escuro; tema escuro inteiro degradado. | `globals.css:39-60` | **alta** | 4 | 2 | **16** |
| A2 | **Erros de auth sem `role=alert`/`aria-live`** — leitor de tela não anuncia falha de login. | `login-form.tsx` | **média** | 3 | 1 | **15** |
| A3 | **`--color-ink-muted` (#7a8fae) falha AA** em texto sobre fundo claro — usado amplamente. | `globals.css:17,1210-1212` | **média** | 3 | 2 | **12** |
| A4 | **Dropdowns (conta/sign-in) hardcodam fundo branco** sem override dark. | `globals.css:687-699,636-651` | **média** | 3 | 2 | **12** |
| A5 | **status-chip warning/success abaixo de AA.** | `globals.css:352-368` | **baixa** | 2 | 2 | **8** |

### 🟢 Receita recorrente / Conversão (motor já existe)

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| R1 | **Destravar pricing de assinatura de curso** — backend já aceita `subscription_monthly/yearly`; UI desabilita com "Coming soon". Receita recorrente quase de graça. | `course-builder-studio.tsx` (un-disable) | **média** | 4 | 2 | **16** |

### 🔵 Estados / Polish

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| P1 | **Auditoria de estados vazio/loading/erro** em `learn/*` e `teach/*` — evita o "parece quebrado" na demo. | `app/learn/*`, `app/teach/*` | **média** | 3 | 3 | **9** |
| P2 | **Redesign do theme-toggle** (#6 do fundador) — swap instantâneo → cross-fade/rotate. A11y já ok. | `components/shared/theme-toggle.tsx` | **baixa** | 2 | 2 | **8** |

### 🟣 Paridade competitiva (Skool engagement + Cakto monetização) — *refinado com evidência primária*

| # | Item | Evidência (eles têm / nós não) | Imp | Esf | Score |
|---|------|--------------------------------|-----|-----|-------|
| C1 | **Loop de gamificação MVP** (likes→pontos→9 níveis→leaderboard 7/30/all-time). | Skool: 1 like=1pt, ladder de 9 níveis (vídeo). Nós: **zero** — "likes" só em copy `capabilities-grid.tsx:36`; `grep level/leaderboard` = 0. Feed existe (`course-community-feed.tsx`) mas sem engajamento mensurável. | 5 | 3 | **15** |
| C2 | **SEO/discovery orgânico** — JSON-LD (Course/Offer) + cursos no sitemap. | Skool = "Google-indexed" (vantagem nomeada). Nós: **0 JSON-LD** (`grep schema.org/@type`=0) + `[slug]` fora do sitemap (`sitemap.ts:10-28`). Fruto baixo, aquisição orgânica grátis. | 4 | 2 | **16** |
| C3 | **Order bump no checkout** (1+ produto, 1-clique). +25% ticket (Cakto). | Encaixa no checkout atual: `line_items` é item único em `index.ts:1416`; flag via `request.data` em `createCheckoutSession` (`index.ts:1300`). | 4 | 3 | **12** |
| C4 | **Gating por nível** ("unlock chat/post/curso ao subir") — *fase 2 do C1, o "dente" da retenção*. | Skool plugins (vídeo t=01:20) + course access "Members of a certain level" (t=01:17). Amarra gamificação→monetização + mata DM-spam. Depende do C1. | 4 | 3 | **12** |
| C5 | **Discovery ranking/sort** (trending/featured/sort UI no catálogo). | Skool ordena por membros/atividade. Nós: "featured"=slice fixo dos 6 (`featured-courses.tsx:12`), sort só A→Z (`published-courses.ts:43`), sem trending. | 4 | 3 | **12** |
| C6 | **Programa de afiliados** (links, atribuição, comissão recorrente). | Skool 40% recorrente + "You were referred by…" no checkout. Nós: **0** (`grep affiliate`=0), MAS ledger+transfer Stripe reusável (`index.ts:2039-2280`); só falta atribuição + 2º leg de payout. | 5 | 4 | **10** |
| C7 | **Members directory** (roster, perfis, níveis, online). | Skool: aba Members. Nós: **stub** hardcoded (`course-community-feed.tsx:254-263`). | 3 | 3 | **9** |
| C8 | **Posts fixados + replies aninhados** no feed. | Skool: pin + threading. Nós: "announcement" é só categoria (sem pin, `grep pinned`=0); comentários single-level, "Reply" posta top-level (`course-community-feed.tsx:475`). | 3 | 3 | **9** |
| C9 | **Upsell/downsell pós-compra 1-clique** (ofertas sequenciais). | Cakto. **Exige primitive que não existe:** `paymentIntents.create`=0 hits, checkout usa `customer_email` only ("not a persistent Customer", `index.ts:2946`). Construção BE maior. | 3 | 4 | **6** |
| C10 | **Quizzes/assessment runtime**. | Skool 2025. Nós: `quiz` é tipo de lição mas runtime é placeholder (`enrolled-course-workspace.tsx:722`); sem modelo question/answer/grade. | 3 | 4 | **6** |
| C11 | **Checkout builder (drag-drop + CRO)** — editor de checkout com temas/fontes/cores, componentes de escassez/prova-social/exit-popup, e múltiplos checkouts por produto (A/B). | Cakto (ao vivo): `checkout-builder/...` drag-drop, exit-popup/notificação/chat, cronômetro/depoimento. Nós: Stripe Checkout hospedado fixo (`createCheckoutSession` `index.ts:1300`), 1 checkout/curso, zero customização. | 3 | 5 | **3** |
| C12 | **Multi-preço / multi-oferta por produto** (várias ofertas no mesmo produto). | Cakto (ao vivo): até 10 preços/ofertas por produto. Nós: 1 preço/curso, sem tabela de ofertas. | 2 | 4 | **6** |

### ⚪ Hardening (regras / storage)

| # | Item | Arquivo:linha | Sev | Imp | Esf | Score |
|---|------|---------------|-----|-----|-----|-------|
| H1 | **`courses` read torna `in_review` world-readable** — verificar se é intencional (sell-on-submit deixa `in_review` comprável; mas pode expor cursos não-listados). | `firestore.rules:1442-1450` | **baixa** | 2 | 2 | **8** |
| H2 | **`validUpload` aceita octet-stream/zip 500MB** sem cross-check com asset-doc. | `storage.rules:48-75` | **baixa** | 2 | 3 | **6** |

---

## 🏆 TOP 10 (por Score)

| Rank | # | Item | Camada | Sev | Imp | Esf | Score |
|------|---|------|--------|-----|-----|-----|-------|
| 1 | T1 | Reconciliar payout (30/10/7) + alinhar copy | Verdade | alta | 5 | 1 | **25** |
| 2 | B1 | Webhook idempotency claim-before-commit | BE money | alta | 5 | 2 | **20** |
| 3 | T2 | Stubs de professor no nav (esconder/rotular) | FE/Verdade | média | 4 | 1 | **20** |
| 4 | B2 | Overwrite de pedido pago fora de ordem | BE money | alta | 4 | 2 | **16** |
| 5 | R1 | Destravar assinatura de curso (BE pronto) | Receita | média | 4 | 2 | **16** |
| 6 | C2 | SEO JSON-LD + cursos no sitemap (discovery orgânico) | Competitivo | — | 4 | 2 | **16** |
| 7 | A1 | Tokens de dark theme nunca trocam (CTA falha AA) | A11y | alta | 4 | 2 | **16** |
| 8 | C1 | Loop de gamificação MVP (paridade Skool) | Competitivo | — | 5 | 3 | **15** |
| 9 | T3 | `/promise` — marcar não-shipado como "em breve" | Verdade | média | 3 | 1 | **15** |
| 10 | A2 | Erros de auth sem `aria-live`/`role=alert` | A11y | média | 3 | 1 | **15** |

> **Cluster de correção de dinheiro (tratar como bloco, mesmo fora do TOP 10 por score):** B1 (20) · B2 (16) · **B3 — guard de compra duplicada não-transacional (12, #11)**. Os três são severidade **alta** e tocam fluxo de caixa LIVE; B3 fica logo abaixo do corte só pelo esforço, não pela gravidade.

---

## Perguntas abertas para o fundador

1. ✅ **Payout (Q1) — RESPONDIDO 2026-06-06:** manter **30 dias** + copy honesta. **T1 executado** (docs/comentários reconciliados; UI já estava em 30).
2. **Stubs de professor (T2):** esconder os 4 do menu na demo, ou manter com rótulo "roadmap Q3 + me avise"?
3. **`/promise` (T3):** enxugar para o shipado, ou manter como visão com tag "em breve"?
4. **Assinatura de curso (R1):** habilitar já para a demo (backend pronto) ou segurar?
5. **Order bump (C3):** formato Cakto (bump único) ou múltiplos bumps? Define o desenho.
6. **Prioridade de paridade competitiva:** gamificação (retenção) primeiro, ou afiliados (aquisição) primeiro? São os dois big bets.

---

## Gate de segurança (antes de qualquer mudança que toque Stripe LIVE)

Passe dedicado, **fora** deste backlog de produto, recomendado antes de mexer no fluxo de pagamento em produção:
`testing-api-security-with-owasp-top-10` · `implementing-pci-dss-compliance-controls` · `implementing-secrets-scanning-in-ci-cd` · `integrating-dast-with-owasp-zap`.

---

## 🚦 FASE 4 — EM EXECUÇÃO (aprovado 2026-06-06). Ordem: T1 → B1 → T2 → opt(a) → B2/B3 → …

Sequência aprovada pelo fundador. Execução **um item por vez**. Em 2026-06-06 o
fundador liberou deploy (@devops pode deployar) e mandou **seguir bloco a bloco
até concluir tudo**, escolhendo o opcional mais recomendado.

| Ordem | Item | Status |
|-------|------|--------|
| 1 | **T1 — payout truth** | ✅ **CONCLUÍDO + DEPLOYADO** (2026-06-06): UI já em 30; docs/comentário reconciliados; Q1=30. |
| 2 | **B1 — webhook idempotency** (claim-before-commit) | ✅ **CONCLUÍDO + DEPLOYADO** (2026-06-06): two-phase `processing`→`done`; só short-circuita em `done`; retry reprocessa marker órfão. Lógica DI testável em `payment-rules.ts` (`claimStripeEvent`/`markStripeEventDone`/`decideStripeEventClaim`), wired em `index.ts`, função antiga removida. 5 testes de regressão + tsc + eslint limpos. |
| 3 | **T2 — stubs de professor no nav** | ✅ **CONCLUÍDO + DEPLOYADO** (2026-06-06): premissa do backlog estava **stale** — os 4 stubs já estavam fora do menu (`contexts: []`, filtro provado) e já rotulados "on the roadmap". Defeito real encontrado e corrigido: link enganoso no **Termos de Serviço**. |
| 4 | **opt(a) — "Notify me" nos painéis roadmap** | ✅ **CONCLUÍDO** (2026-06-06): opcional mais recomendado vs. opt(b) (deletar `/teach/refunds` criaria 404 p/ URL digitada; redirect gracioso é melhor). `TeacherComingSoonPanel` ganhou prop `notifyFeature` → CTA `mailto:support` por feature, convertendo dead-end de roadmap em sinal de demanda mensurável. Presentational, atrás do gate `teacherStudio.access`. Commit `d5ebdca`. |
| 5 | **B2 + B3 — overwrite fora de ordem + guard de compra duplicada** | ✅ **CONCLUÍDO** (2026-06-06): bloco de dinheiro fechado. B2: `markOrderStatus` transacional + freeze de estado terminal (`shouldApplyOrderStatusTransition`). B3: lock de checkout in-flight reivindicado **dentro de transação** (serialização OCC) + `expires_at` na sessão Stripe + release de lock com guarda de posse (`shouldReleaseCheckoutLock`). Review adversarial pegou um race de double-charge no path de takeover **antes do LIVE**; corrigido e re-verificado (SAFE-TO-DEPLOY). tsc + **106/106** vitest + eslint limpos. Commit `0ca1ecf`. |
| 6 | **C2 — SEO/discovery orgânico (JSON-LD + sitemap)** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): escolhido como o item de maior ROI do bloco de paridade (fruto baixo, aquisição orgânica grátis, score 16). JSON-LD `Course` (+`Offer`) nas páginas de catálogo estático + 6 rotas `/courses/[slug]` no sitemap + dedup de `SITE_URL`. **Review adversarial pegou um furo de No-Invention (Art. IV) antes do LIVE:** a 1ª versão hardcodava `availability:InStock` sempre que havia preço — o `Offer` foi **gateado por `purchasable`**, derivado do **mesmo `getCourseAccessDecision`** que o CTA renderiza, garantindo paridade structured-data ↔ conteúdo visível em ambas direções. tsc + **115/115** vitest (incl. regressão purchasable=false) + eslint limpos. Commit `7e7db0c`. |
| 7 | **T3 — `/promise` promete o não-shipado** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): commit anterior `257ff4c` já tinha tirado custom-domains/analytics/affiliate do "feature parity", mas **manteve quizzes** alegando "verified live" — verificação independente provou que era **falso**: `quiz` é um `LessonType` selecionável (`learning.ts:8`, `teacher-course.ts:46`), mas o lado do aluno é stub (`enrolled-course-workspace.tsx:722`: "Quiz questions and passing rules will appear here **when assessment tools are connected**") — zero UI de quiz-taking/grading/passing-rules. Removido "quizzes" do promise #02; certificates/drip/community **verificados shipped** (ficam). Quiz builder é **latente** (flag `teacherStudio.courseBuilder` off em prod, sem override) → sem impacto live em aluno hoje. LIVE: `/promise` sem nenhum "quiz". Commit `49a4e29`. |
| 8 | **A1 — contraste AA do tema escuro** | 🟡 **SUBSET CRÍTICO CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): 3 fixes de contraste WCAG-verificados (luminância sRGB, zero regressão). (1) **CTA primário** `.button-solid` — `--color-primary` inverte p/ azul-claro `#8fb4e4` no escuro mas o label era branco hardcoded → **2.14:1** (exatamente o sintoma "CTA primário falha AA no escuro"); trocado p/ tinta escura → 6.1–9.0:1. (2) chevron de collapse da sidebar — mesmo bug, dark no rest / branco no hover-vermelho. (3) número de step "done" — branco sobre `#1f8a5b` era **4.33:1** (falhava nos DOIS temas) → novo `--color-success-solid #157f50` (5.02:1). `next build` ✅ · LIVE: bundle CSS contém as 3 regras. **DEFERIDO (A1-cont, ver registro técnico):** os tokens semânticos de FOREGROUND (accent/success/warning/danger) ainda não trocam no escuro (texto vermelho/verde ~2.5:1) — fix seguro exige decouple fg/solid + tematizar ~12 superfícies branco-hardcoded. Commit `a34f29e`. |
| 9 | **A2 — erros de auth sem `role=alert`/`aria-live`** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): leitor de tela ficava **mudo** em falha de login/signup/reset (mensagem era `<p>` puro, sem live region). Escopo ampliado p/ toda a superfície de credenciais (3 forms, não só o nomeado): erro → `role="alert"` + `aria-live="assertive"` (interrompe — falha é urgente); sucesso do reset → `role="status"` + `aria-live="polite"` (não interrompe). Anúncio na inserção é confiável (erro aparece em form estável pós-submit, sem troca de rota). Zero mudança visual/layout. `next build` EXIT=0 + **115/115** vitest + eslint limpos · LIVE: chunks de `/login` e `/forgot-password` contêm `role="alert"`+`aria-live=assertive` (e o de reset, `role="status"`+`polite`). Commit `0979b29`. |
| 10 | **A3 — `--color-ink-muted` falha AA** (texto secundário) | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): o token claro `#7a8fae` (usado em ~40 lugares p/ labels/placeholders/sub-texto) **falhava AA-normal em TODA superfície clara** — medido: 3.30:1 no branco, 2.98:1 surface-soft, 2.81:1 surface-strong (pior que o backlog sugeria). Escurecido por **0.74× uniforme** (hue idêntico, ratios de canal 0.738/0.741/0.741) → `#5a6a81`: 5.51:1 branco / 4.98:1 soft / 4.68:1 strong, ainda um degrau mais claro que `--color-ink-soft`. Auditadas as 16 usos (7 CSS + 9 JSX): **nenhum** coloca texto muted sobre fundo escuro/colorido → zero regressão (chips `draft`/`inactive` têm bg = tint 8-12% da própria cor sobre branco, então contraste só melhora: 4.90/5.09:1). Dark theme mantém seu próprio `#6b7e9a` (intocado). `next build` EXIT=0 · LIVE: bundle CSS tem `#5a6a81`, `#7a8fae` sumiu, `#6b7e9a` intacto. Commit `6fe39b6`. |
| 11 | **A4 — dropdowns conta/sign-in hardcodam fundo branco** (sem override dark) | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): ambos os painéis (`.signin-dropdown`, `.account-menu-panel`) tinham `background:#ffffff` fixo → no dark, o texto de token (`--color-ink` #e8edf5, que troca) ficava **~1.1:1 numa caixa branca = invisível**. Tokenizado bg → `var(--color-surface)` (=#fff no claro, surface escura no dark) + borda do signin-dropdown → `var(--color-line)`. Como escurecer o painel **moveu** os elementos accent-red que **não trocam** (Sign out texto+ícone, context-chip) e os section-labels ink-muted dark p/ fundo escuro, adicionados overrides `[data-theme=dark]` pareados: vermelho `#b22234` (2.73:1) → `#e06472` (5.19–5.36:1), section-label → ink-soft (8.48:1). O **trigger** (pílula translúcida branca que deixava o nome ink-claro invisível no dark) invertido p/ tint claro translúcido; role-red → `#e06472` (5.00:1). **Todos os elementos do account-menu agora 5.00–15.36:1 no dark** (compositado+verificado). Claro **intocado** (todos overrides são dark-scoped). `next build` EXIT=0 · LIVE: bundle tem `#e06472`, `.account-menu-panel{…var(--color-surface)}`, `[data-theme=dark] .account-menu-trigger{background:#ffffff0f}`. Commit `c41666f`. |
| 12 | **A5 — status-chip warning/success abaixo de AA** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): medidos os 6 chips (10px/700 = normal text, precisa 4.5:1) nos 2 temas. **Claro:** só **warning (3.10:1)** e **success (3.78:1)** falhavam — exatamente o nomeado; draft/inactive **agora passam** (4.89/5.09:1) graças ao A3 ter escurecido o ink-muted; danger/info já passavam. Como `#1f8a5b` dá só 4.34:1 até no branco, o **texto** tinha que escurecer (clarear o bg não resolve). Texto claro → warning `#8a5d08` (5.16:1), success `#157049` (5.32:1). Esses seriam quase-preto sobre os tints escuros, então overrides `[data-theme=dark]` restauram tints claros: warning `#e6b24f` (8.05:1), success `#5fc99a` (7.85:1). **Resultado: claro corrigido + dark melhorado** (warning 4.51→8.05, success 3.69→7.85). `__dot` usa `currentColor` → segue. Contraste dos outros chips no dark (danger/draft/inactive) fica no A1-cont. `next build` EXIT=0 · LIVE: bundle tem os 4 valores + `var(--color-warning)` removido do chip. Commit `e7a42be`. |
| 13 | **P1 — auditoria de estados vazio/loading/erro** (learn/* + teach/*) | ✅ **CONCLUÍDO + DEPLOYADO** (2026-06-07): auditoria fan-out (2 agentes read-only, learn + teach) concluiu **cobertura forte no geral** — dashboards, hubs, sale-list/detail, course-builder, credentials, wishlist, community já tratam loading/empty/error. **2 gaps reais "parece quebrado" corrigidos** (verificados file:line antes de editar): (1) `teacher-event-studio` mostrava *"No agenda items scheduled yet"* **durante o load** (subscription de eventos sem flag → `events:[]` no 1º render) — professor COM sessões via "vazio" piscando; add `eventsLoaded` → "Loading scheduled sessions…" até entregar. (2) `teacher-wallet-panel` virava `isLoading=false` no profile enquanto orders+ledger ainda pendentes → hero net + balance rows + metric cards (gross/fee/paid) + tabela ledger **piscavam $0** numa tela financeira; add `ordersLoaded`+`ledgerLoaded`, valores monetários mostram `—` até ambos entregarem (resolve p/ número real, ou $0 correto p/ professor sem vendas — sem zero falso). Loaders inline de texto + skeleton-pulse das métricas **mantidos** (estados válidos; SkillsetSpinner tem min-height 280px, grande demais p/ áreas inline). `next build` EXIT=0 + **115/115** vitest + deploy EXIT=0. *Verificação LIVE anônima N/A* (telas auth+`teacherStudio.access`-gated; componentes em chunks lazy fora do shell público — não acessíveis sem credenciais, que não uso por política). Commit `696afff`. |
| 14 | **P2 — redesign do theme-toggle** (cross-fade) | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): premissa "swap instantâneo" estava **parcialmente stale** — o ícone do toggle (Sol/Moon) **já** faz cross-fade+rotate (300ms, `motion-reduce` tratado, `theme-toggle.tsx`). O que de fato **piscava** era a página: trocar `[data-theme]` inverte **todas** as CSS vars de uma vez, então o tema do site inteiro dava um snap duro enquanto só o ícone animava. Fix real: `theme-provider` arma uma classe efêmera `.theme-transitioning` no `<html>` durante o `setMode` (só em ação do usuário → load inicial nunca pisca; timeout ref-trackeado cancela em toggles rápidos), e `globals.css` faz cross-fade de 300ms das props de cor (bg/border/color/fill/stroke/box-shadow) **+ transform/opacity** (preserva a animação própria do ícone), tudo atrás de `prefers-reduced-motion: no-preference`. `next build` EXIT=0 + **115/115** vitest + eslint limpos · LIVE: bundle CSS tem a regra `@media(prefers-reduced-motion:no-preference){html.theme-transitioning …}` e o chunk JS do provider (`07yw_j5~98yb3.js`, 200) tem `classList.add("theme-transitioning")` + o guard `clearTimeout`/`setTimeout`. Commit `4e785b9`. |
| 15 | **C5 — discovery ranking/sort** (slice executável) | ✅ **SLICE CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): o marketplace `/courses` tinha filtro de categoria + busca mas **zero sort** — a lista era sempre A→Z por título (`published-courses.ts:43`). Adicionado **controle de Sort** com opções **ancoradas só em dado já presente no card** (No-Invention): Alphabetical (default, **inalterado** → zero regressão), Top rated (`ratingAverage`/`ratingCount`), Price low→high, Price high→low. Lógica pura extraída p/ `lib/data/course-sort.ts` (testável, fora do client component): não-avaliados afundam por último, sem-preço ("opening soon") afunda nas **duas** direções, título é tiebreaker estável. `CourseCard` ganhou campos numéricos opcionais (`priceAmountMinor`/`ratingAverage`/`ratingCount`) populados pelo mapper de teacher-course. **DEFERIDO (C5-cont, `task_b9fb7f79`):** "trending" (precisa agregação de métrica de atividade — enrollments/views por janela — que **não existe**; `createdAt`/`updatedAt` são `unknown`) e "featured" editorial (decisão de curadoria do fundador). `next build` EXIT=0 + **122/122** vitest (+7 testes de comparador) + eslint limpos · LIVE: chunk `0zh7kk9c-jkgr.js` (200) tem `price-asc/price-desc/Top rated/Price: low to high/Alphabetical` e `/courses` serve 200. Commit `04e03b2`. |
| 16 | **H1 — `courses` read torna `in_review` world-readable** | ✅ **VERIFICADO — INTENCIONAL + CORRETAMENTE ESCOPADO, sem mudança** (2026-06-07): a regra (`firestore.rules:1447`) torna `published` **e** `in_review` world-readable, **documentado** (1443-1446) como **sell-on-submit** — curso recém-submetido fica imediatamente visível+comprável, aprovação não-bloqueante. A preocupação "expõe cursos não-listados" **não se aplica**: (1) **não existe estado "unlisted"** — só se chega a `in_review` via submit explícito do professor (`create` força `draft` `:1455`; só `teacherCanSubmitOwnedCourseForReview` move p/ in_review `:1462`), que sob sell-on-submit **É** a ação de publicar; rejeitado→`needs_changes` e despublicado→`inactive` voltam a privado. (2) só **metadata de catálogo** é exposta — todo conteúdo protegido é gated por ownership/enrollment, **não** por status: `assets` (`:1480`), `lessonComments` (`:1501`), `communityPosts`+comments (`:1568,1585`), `enrollments`+progress (`:1541,1553`). (3) `published`/`in_review` tratados **idênticos** por design (zero leak incremental); `courseReviews` `:1525` idem. **Mudar quebraria** sell-on-submit (anônimo não veria/compraria curso recém-submetido) = regressão de feature deliberada. Consistente com data layer (`published-courses.ts:31`) + UI. Sem código, sem deploy. |
| 17 | **H2 — `validUpload` hardening** (storage) | ✅ **SLICE CONCLUÍDO + DEPLOYADO + VERIFICADO (emulador)** (2026-06-07): auditoria de `storage.rules:48-75` achou que `validUpload` aceitava `text/.*` p/ lesson materials — **incluindo `text/html`**: um HTML armazenado, aberto por um aluno matriculado via blob URL (`course-assets.ts:156-159` faz `getBlob`→`URL.createObjectURL`), executaria script no contexto do viewer (XSS armazenado professor→aluno) — **o mesmo risco de markup ativo que a regra já barra p/ `image/svg+xml`**. Bloqueado `text/html` (variantes com charset cobertas via `matches` negado); plain/markdown/csv continuam passando (não quebra material real — confirmado em `course-asset.ts:113-119`). **2 testes de emulador** adicionados (html rejeitado + text/plain aceito); **32/32** rules tests passam (`npm run test:rules`). Deploy `firebase deploy --only storage`: "compiled successfully" + "released". **DEFERIDO (H2-cont, `task_e346eb48`):** o fix *literal* do backlog (cross-check vs asset-doc) **não é drop-in** — `uploadCourseAsset` grava o blob no Storage **ANTES** de criar o doc Firestore (`course-assets.ts:85` antes de `:125`), então exigir doc pré-existente quebraria **todo** upload; precisa re-arquitetura doc-first (com handling de orphan), decisão de arquitetura surfaçada. Commit `dae7d78`. **→ H2-cont RESOLVIDO (2026-06-07):** após pesquisa de mercado + auditoria, decisão **Opção C** — o vetor real já está fechado (fatia H2), o residual é integridade/órfão de severidade baixa (cliente já faz GC na falha), e doc-first/signed-URL é re-arquitetura desproporcional de um core money-adjacent funcionando; signed-URL fica como **upgrade-path documentado** (gatilho: quota/malware-scan/CDN). Sem código. Ver **registro da DECISÃO** abaixo. |
| 18 | **R1 — assinatura de curso (build faseado p/ valer)** | ✅ **CONSTRUÍDO + DEPLOYADO** (2026-06-07): a premissa "UI flip" do backlog era **falsa** (4 camadas de bloqueio — ver registro de **verificação** abaixo); o fundador escolheu **"construir de verdade (faseado)"** com 3 decisões financeiras ancoradas em mercado (repasse **held** ≥ janela de reembolso reusando `payoutLedger`+`dailyReleaseTransfers`; **período de graça** em falha de pagamento; cadências **mensal + anual**). Construído em **5 fases** (commits `7b6cba5` / `89598ca` / `5faf550` / `bd428c1` / `525b293`): checkout `mode:'subscription'` na conta **PLATAFORMA** + `separate_charges_and_transfers` (economia idêntica à one-time, **sem** application_fee) → `invoice.paid` grava `payoutLedger` **held** + concede enrollment `source:'subscription'` → lifecycle revoga/restaura em cancel/past_due/deleted → callable cancel/resume (`cancel_at_period_end`) + card live do aluno → gate de submit + opções da UI **destravados**. tsc (FE+functions) + eslint + **35/35** rules tests limpos; `deploy:app` + `deploy:rules` EXIT=0 (`cancelCourseSubscription` criada, `stripeWebhook`/`createCheckoutSession`/`validateCourseReadyForReview` atualizadas). **1 passo manual de go-live pendente (fundador/@devops):** adicionar **5 eventos de assinatura** ao endpoint do webhook Stripe (`invoice.paid`, `invoice.payment_failed`, `customer.subscription.created/updated/deleted`) — sem eles a assinatura **cobra mas não concede/revoga acesso** (`STRIPE_CHECKLIST.md` atualizado p/ 9 eventos). Ver **registro técnico do BUILD** abaixo. |
| 19 | **C1 — loop de gamificação MVP** (likes→pontos→9 níveis→leaderboard) | ✅ **CONSTRUÍDO + DEPLOYADO** (2026-06-07): maior item competitivo aberto (score 15). Premissa **verificada TRUE antes de construir** (grep `level`/`leaderboard`=0; feed existe sem engajamento mensurável). Construído **server-authoritative** (cliente nunca escreve pontos) em **3 fases** (commits `0577584` / `f4e608a` / `635c8da`): **F1** engine de pontos — likes como subcoleção `communityPosts/{id}/likes/{likerId}` (doc-id=uid → 1 like/usuário, anti-duplo-voto), triggers `onCommunityLikeCreated`/`Deleted` fazem RMW transacional em `memberStats/{uid}` (deriva nível da ladder Skool `[0,5,20,65,155,515,2015,8015,33015]` p/ pontos/nível nunca divergirem; pula self-like; clamp em 0) + ledger append-only `pointsEvents`; **F2** cron `rebuildLeaderboards` (6/6h) precomputa `leaderboards/{7d,30d,all-time}` top-20; **F3** UI — tab Leaderboard (toggle 7d/30d/all-time), badge "Lv N" em autores de post/comment, botão de like (Heart + contagem, desabilitado no próprio post com dica "Likes on your posts become points."). **Pontos só de likes RECEBIDOS** (1 like=1pt; postar **não** dá ponto — anti-farm, padrão Skool). tsc FE + functions(`npm run lint`) + eslint limpos; **42/42** rules tests (firestore 32 + storage 10; +7 testes C1 de anti-cheat); `deploy:rules` (Phase 1) + `deploy:app` EXIT=0 (`onCommunityLikeCreated`/`onCommunityLikeDeleted`/`rebuildLeaderboards` live + hosting released). Ver **registro técnico do BUILD** abaixo. |
| 20 | **A1-cont — contraste AA dos tokens semânticos de foreground (dark mode)** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO LIVE** (2026-06-07): continuação do subset A1. Auditoria (workflow 8-agentes Extract→Audit→Design→Verify) confirmou que `[data-theme="dark"]` remapeava superfícies mas **nunca** sobrescrevia os tokens de FOREGROUND semânticos → texto/ícone vermelho (`--color-accent #b22234`) e verde (`--color-success #1f8a5b`) sobre superfícies escuras media **~2.73:1** (reprova AA 4.5:1). Estratégia **fail-safe `-fg`**: tokens novos `--color-accent-fg`/`--color-success-fg` **= valor base no `:root`** (zero mudança no light, garantido) e clareados **só** sob `[data-theme="dark"]` (`#e36b78`/`#5fc99a`). **260 migrações fail-safe** (16 regras em `globals.css` + 244 utilitários em **94** `.tsx`: 241 accent + 3 success) via script idempotente por **prefixo de foreground** (`text-`/`border*`/`decoration`/`ring`/`outline`/`fill`/`stroke`/`caret`/`placeholder`) — **nunca** toca `bg-`/`accent-`/gradiente/`text-white`, então fills sólidos (badge, hover de sidebar, group-hover CTA, accent de form-control) ficam saturados e um ponto de texto perdido **fica no status quo, nunca regride**. accent-fg `#e36b78` mede **4.65→6.06:1** (surface-strong→base); success-fg `#5fc99a` **7.2→9.4:1**. **No-Invention catch:** o modal de lição tem bg `linear-gradient(#ffffff → surface-soft)` (bolsão claro-no-escuro) → 4 linhas de accent **excluídas** da migração e **deferidas (A1-cont-2)**. tsc FE EXIT 0; `next build` EXIT 0; deploy `deploy:app` EXIT 0 (hosting released). **LIVE (prod):** bundle servido contém `[data-theme=dark]{…--color-accent-fg:#e36b78}` + `--color-success-fg:#5fc99a` + `.status-chip--danger{color:var(--color-accent-fg)}`. Commit `489e293`. Ver **registro técnico** abaixo. |
| 21 | **A1-cont-2 — white-on-primary + dark theme do modal de lição** | ✅ **CONCLUÍDO + DEPLOYADO + VERIFICADO** (2026-06-07): fecha a thread de AA do dark mode (o resto deferido do A1-cont). Auditei direto em `globals.css` (No-Invention) as 13 superfícies `background: var(--color-primary*)` — `--color-primary` inverte de navy `#1a365d` (light) p/ azul-claro `#8fb4e4` (dark). **Parte 1 — 4 falhas white-on-primary** (texto/ícone branco que vira branco-no-claro): sub-labels `.account-settings-tab--active`/`.course-upload-preset--active` (rgba branco 76%), indicador `.create-course-payment.is-active>span` (#fff), ícone `.learner-continue-card__icon` (#fff) → dark-scoped p/ `var(--color-base)` (tinta escura que inverte, **8.8:1** sobre o primary claro), mesmo precedente do `.button-solid`/`.dash-card--accent`. Toggles (knob por posição) e o hero-cover (texto vive em overlay com bg escuro próprio) **não** são falha — verificado e descartado. **Parte 2 — modal de lição (falha real, não estética):** o bg é `linear-gradient(#ffffff → surface-soft)` cujo **topo fica branco no dark**, enquanto os tokens de texto (`--color-primary`/`--color-ink`/`--color-ink-soft`) invertem p/ claro → h3 azul-claro **~1.9:1** e corpo slate-claro **~2.0:1** sobre os bolsões brancos. Re-tematizei o modal dark (1 bloco consolidado, 13 overrides): chrome (header/tabs/footer/close) → surface translúcida; body → camada mais escura (surface→base) p/ os cards de conteúdo (surface-strong) **levantarem**; card `-video` (gradiente primary-dark→primary, que inverte claro) → gradiente escuro p/ o texto branco se manter; knob do on-toggle → tinta escura (visível no track azul-claro, reforça o estado); accent/success internos (crumb/eyebrow, tab-active border, upload error/success) → `-fg`. Tudo dark-scoped → **light mode byte-idêntico**. `npm run build` EXIT 0 ("Compiled successfully"); deploy `deploy:app` EXIT 0 (functions Skipped — só CSS; hosting released). **Verificado:** os 6 marcadores presentes no artefato deployado (`.firebase/.../hosting/.../0a4r_7xbq_fei.css`, byte-idêntico ao upload) + HTML prerenderizado referencia esse chunk. Commit `7aabb9a`. Ver **registro técnico** abaixo. |
| 22 | **C5-cont (Fase 1) — featured editorial ops-gated** | ✅ **CONCLUÍDO + DEPLOYADO** (2026-06-07): resolve a 1ª das duas partes deferidas do C5 (a decisão de curadoria "featured"). Pesquisa de mercado (Coursera/Udemy/Skool: default curado + sorts explícitos honestos) → design **"featured" como MODO de sort** ("Featured & top picks", **novo default**), **não** pin cross-sort: enquanto nada está featured = byte-idêntico ao A→Z histórico (**zero regressão**), e sorts explícitos (rating/price) ficam **puros** (sort explícito = exatamente aquilo, sem featured furando a fila). Curadoria **ops/admin-only** (`firestore.rules` `opsCanUpdateCourseFeatured` field-scoped antes do `validCourseShape` p/ orçamento de expressões; professor **não** auto-featura — `featured` fora do `courseChangedOnly` dele); `featuredRank` validado (int 0–1M ou null). UI ops: toggle Feature/Unfeature + badge "Featured" no `ManagedCoursePanel` (só em `published`). `next build` EXIT=0 + **128/128** vitest (+6 testes featured de `course-sort`, total 13) + **48/48** rules tests (+6 featured: ops/admin podem, professor/aluno não, smuggle bloqueado, rank malformado rejeitado) + eslint limpos; `deploy:rules` EXIT=0 (firestore released) + `deploy:app` EXIT=0 (SSR atualizada, 28 functions Skipped). Commit `52de8db`. Ver **registro técnico** abaixo. |
| 23 | **C5-cont (Fase 2) — trending now (enrollment-velocity)** | ✅ **CONSTRUÍDO + DEPLOYADO** (2026-06-07): fecha o thread C5 — a 2ª parte deferida (o "trending" que exigia dado de atividade inexistente, agora construído). Trending = **matrículas nos últimos 7 dias** (sinal de popularidade mais simples e honesto, espelha o "ordena por atividade" do Skool). **2 campos server-only no course doc:** `trendingScore` (janela 7d, recomputado pelo cron `rebuildTrending` 6/6h direto da coleção `enrollments` — **recount idempotente/self-healing**, trigger perdido/duplicado nunca corrompe o ranking) + `enrollmentCount` (lifetime, bump do trigger `onEnrollmentCreated` em cada matrícula nova; re-ativações são updates, não contam duplo). Ambos **Admin-SDK-only** (fora de todo `courseChangedOnly` → nem professor nem ops escrevem). "Trending now" entra como **sort explícito** (2ª opção, depois de Featured) — **não** vira default e **não** pina (coerente com a decisão da Fase 1); sem atividade colapsa p/ A→Z (no-op gracioso). Lógica de agregação extraída p/ módulo puro testável `course-trending.ts`. functions tsc + **143/143** vitest (+10 de `course-trending` + 5 de `course-sort` trending) + **49/49** rules tests (+1 server-only) + eslint limpos; `deploy:rules` EXIT=0 + `deploy:app` EXIT=0 (**`onEnrollmentCreated` + `rebuildTrending` criadas live**, hosting released). Ver **registro técnico** abaixo. |

### R1 — registro técnico da VERIFICAÇÃO (2026-06-07) — histórico (premissa falsa, pré-build)

- **Por que parei antes de tocar:** R1 toca o **caminho do dinheiro** (checkout→Stripe→webhook→enrollment). A regra No-Invention exige verificar toda alegação antes de afirmar/agir; o backlog (baseado em finders) pode errar. Verifiquei **antes** de mudar qualquer coisa — e a premissa central ("BE pronto") é factualmente falsa.
- **Evidência file:line (4 camadas de bloqueio):**
  - **UI:** `course-builder-studio.tsx:302-319` — `subscription_monthly`/`subscription_yearly` com `disabled:true` + `badge:"Coming soon"`.
  - **Submit gate (servidor):** `index.ts:548-553` — `validateCourseReadyForReview` lança `failed-precondition` p/ qualquer `paymentType !== 'one_time'` (exceto free). Um professor que escolhesse assinatura tomaria erro duro ao submeter.
  - **Checkout (servidor):** `index.ts:1497-1518` — `createCheckoutSession` é `mode:"payment"` fixo, `line_items` com `price_data` one-time (`unit_amount`), Connect order `payoutModel:"separate_charges_and_transfers"` (`:1488`). Nenhum branch de subscription, nenhum `recurring`, nenhum `subscription_data`.
  - **Enrollment/webhook:** o sistema de subscription existente (`session.mode==='subscription'` `:2976`; `customer.subscription.*` `:3005-3015`; `syncSubscriptionFromStripe` `:3735`; coleção `subscriptions`) serve **só o plano SaaS do professor** — resolve priceId→plano Skillset (`:3496`), grava entitlement de plano, **não** cria/revoga enrollment de curso. Um course-subscription cairia nesse path e seria mis-tratado como plano.
- **Conclusão:** "un-disable" é inviável; mesmo removendo o gate, o checkout cobraria **uma vez** (não recorrente) → quebrado semanticamente. O build real é Stripe Connect **recurring** com split por application_fee, ciclo de vida de assinatura espelhado em enrollment, cancelamento, e revisão do modelo de payout/clearing (o cron atual assume ordem one-time com janela de 30d). É o maior item de money-path do backlog, não um flip.
- **Ação:** premissa corrigida aqui (higiene de verdade, espírito T1/T3). **Decisão do fundador pendente** sobre comissionar o build real (com as decisões financeiras que ele carrega) vs. alternativa de receita recorrente. Sem código, sem deploy.

### R1 — registro técnico do BUILD (2026-06-07) — construído de verdade, faseado, deployado

- **Decisão do fundador:** "Construir de verdade (faseado)". As 3 decisões financeiras, ancoradas na prática das grandes plataformas (research adversarial, não o "ideal"):
  - **Repasse NÃO imediato — held até a janela de reembolso.** Reuso do trilho **já existente**: `payoutLedger` (held) + cron `dailyReleaseTransfers`, `separate_charges_and_transfers`, janela `payoutReleaseDelayDays` (default 30, mín 7), mesmo `platformFeeBps` por ciclo. Confirmado por Hotmart (~D+30 + reserva 10%), Kiwify (D+30 default / D+2-D+7 pago + reserva 10%, reembolso CDC 7d), Skool (payout semanal). **Economia idêntica à one-time** — o professor recebe cada fatura após a janela, não na hora.
  - **Falha de pagamento → período de graça** (prática de mercado, menor prejuízo p/ todos). Enrollment **continua ativo** durante o dunning do Stripe (`past_due`); revoga só em `canceled`/`unpaid`/`incomplete_expired`/`paused`. (Kiwify: 5 dias de retry e então cancela.)
  - **Cadências:** mensal **e** anual (ambos enums `subscription_monthly`/`subscription_yearly`).
- **Arquitetura (verificada file:line, No-Invention):** assinatura Stripe `mode:"subscription"` na conta **PLATAFORMA** com Customer persistente (`getOrCreateBillingStripeCustomer`, reusado); **recurring Price por curso** mintado+cacheado lazy (`getOrCreateCourseSubscriptionPrice`, `index.ts:640` — Price imutável, re-minta em mudança de preço/cadência/moeda, cacheado em `course.stripeSubscriptionPrice`); `separate_charges_and_transfers` (**não** destination charges / `application_fee_percent`) → cada fatura paga vira Transfer **held** no `payoutLedger`, liberado pelo `dailyReleaseTransfers` após a janela. Metadata da subscription carrega `purpose:"course_subscription"`, courseId, userId, teacherId, connectedAccountId, platformFeeBps, currency — é o que **desambígua** course-sub (→ handlers de curso) de plano SaaS do professor (→ `syncSubscriptionFromStripe`).
- **As 5 fases (1 commit cada):**
  - **F1 `7b6cba5`** — checkout: branch de subscription em `createCheckoutSession` (`courseSubscriptionInterval(paymentType)` → `month`/`year`), Customer plataforma + Price recorrente, `subscription_data.metadata` com `purpose:course_subscription`, idempotencyKey. Type `stripeSubscriptionPrice` + helper de Price cacheado.
  - **F2 `89598ca`** — fulfillment: `handleCourseSubscriptionInvoicePaid` (`index.ts:3456`) — no-op salvo `purpose=course_subscription`; recomputa `platformFeeBps` do plano **atual** do professor (`canonicalPlatformFeeBpsForPlan` → upgrade p/ Plus aplica na renovação); transação grava `payoutLedger/{invoice.id}` (status `in_release`, `releaseAt` = +janela, `kind:course_subscription`) se gross>0 && connectedAccount; concede/reativa enrollment `source:"subscription"`; espelha `courseSubscriptions/{subId}`. Wired em `invoice.paid`.
  - **F3 `5faf550`** — grace + lifecycle: `handleInvoicePaymentFailed` ramifica course-sub → grava `pastDue` e **retorna** (mantém acesso); `handleCourseSubscriptionLifecycle` (`:3680`) — entitled em `active`/`trialing`, revoke em `canceled`/`unpaid`/`incomplete_expired`/`paused`; revoga active→revoked ou restaura revoked→active; **nunca** cria enrollment. Roteado em `customer.subscription.created/updated/deleted` com fallback p/ `syncSubscriptionFromStripe` (plano).
  - **F4 `bd428c1`** — cancel/resume + UX do aluno: callable `cancelCourseSubscription` (verifica posse via `metadata.userId===uid` + `purpose`, toggle `cancel_at_period_end`); `subscriptionId` carimbado no enrollment doc (id determinístico `${uid}__${courseId}`, evita índice composto); `CourseSubscriptionCard` live-lê `courseSubscriptions/{id}` (own-read rule) — renova/cancelamento agendado/past_due + cancel/resume. Regra Firestore `courseSubscriptions` (own-read, deny-write) + 3 testes.
  - **F5 `525b293`** — ativação (go-live): gate `validateCourseReadyForReview` aceita subscription_monthly/yearly (allowlist, preço>0); UI destrava as 2 opções (remove `disabled`+"Coming soon"), `pricingModelIsReady` trata qualquer pago como pronto sse preço>0; página do aluno mostra `/ month`·`/ year` e CTA "Subscribe for …".
- **`hasEnrollmentForCourse`** gateia em status `[active, completed]` → `revoked` corta acesso a asset protegido automaticamente (sem código extra).
- **Verificação:** tsc FE (`tsc --noEmit -p tsconfig.json`) EXIT 0; tsc functions (`npm run lint`) EXIT 0; eslint dos arquivos tocados EXIT 0 (1 fix: `set-state-in-effect` no card → remontagem por `key`); **35/35** rules tests (`npm run test:rules`, +3 de `courseSubscriptions`). Deploy `deploy:app` EXIT 0 (`cancelCourseSubscription` **create** OK, demais **update** OK, hosting released) + `deploy:rules` EXIT 0 (firestore released). *Verificação E2E LIVE de cobrança N/A por política* (exige cartão/conta no checkout Stripe) — teto de verificação = build/deploy verdes + rail reusando caminho one-time **já verificado em produção** ($100 → professor $88.80 / plataforma $8.00).
- **⚠️ Passo manual de go-live (NÃO feito — config de webhook é permission-gated; fundador/@devops):** o endpoint Stripe documentado assinava **4 eventos** (one-time/refund); o rail precisa de **+5** (`invoice.paid`, `invoice.payment_failed`, `customer.subscription.created`/`updated`/`deleted`). **Sem eles, uma assinatura cobra o aluno mas o webhook nunca dispara fulfillment/lifecycle** → sem matrícula, sem payout ledger, sem revogação. `STRIPE_CHECKLIST.md` + `BLOCKERS.md` atualizados (4→9 eventos, com file:line dos handlers). Os mesmos 5 também alimentam o lifecycle do **plano SaaS do professor** (se nunca foram adicionados, esse lifecycle também não dispara hoje).

### C1 — registro técnico do BUILD (2026-06-07) — gamificação MVP, server-authoritative, faseado, deployado

- **Premissa verificada TRUE antes de construir (No-Invention):** ao contrário do R1, a premissa do backlog ("zero gamificação") era **factualmente correta** — Explore agent confirmou `grep level/leaderboard`=0, "likes" só em copy (`capabilities-grid.tsx:36`), feed real existe (`course-community-feed.tsx`) mas sem nenhum sinal de engajamento mensurável. Sem bloqueio → construído.
- **Decisão de mercado (não-money-path, risco menor):** modelo **Skool-standard** (evidência primária de vídeo, FASE 2). **Pontos vêm SÓ de likes RECEBIDOS — 1 like = 1 ponto.** Postar/comentar **não** dá ponto (é farmável — decisão deliberada anti-gaming, idêntica ao Skool). Ladder de **9 níveis** com os thresholds publicados do Skool `[0,5,20,65,155,515,2015,8015,33015]` (canônico em `src/domain/gamification.ts`, espelhado em functions).
- **Arquitetura anti-cheat (server-authoritative — cliente NUNCA escreve pontos):**
  - **Like = subcoleção** `communityPosts/{postId}/likes/{likerId}` com **doc-id = uid do liker** → 1 like por usuário estruturalmente (unlike = delete). Regra Firestore: create gated por `likerId==auth.uid` + matrícula + **shape-locked** (`hasOnly [likerId,postId,createdAt]` → cliente não consegue injetar campo `score`); delete só do próprio. **Sem `likeCount` denormalizado no post** (autor poderia inflar) — usa contagem viva da subcoleção.
  - **`memberStats/{uid}`** (server-only write via Admin SDK, read signed-in) guarda `{uid, displayName, points, level, totalLikesReceived}`. Vive em **coleção própria, não no doc `users`** — evita mexer no allowlist/guard de escalonamento de `users`.
  - **`pointsEvents/{eventId}`** ledger append-only (admin-read, server-only) p/ agregação por janela; **`leaderboards/{window}`** (signed-in read, server-only) precomputado.
- **As 3 fases (1 commit cada):**
  - **F1 `0577584`** — engine: imports `onDocumentCreated`/`onDocumentDeleted`; `applyCommunityLikeDelta(postId, likerId, delta)` lê o post p/ `authorId`, **pula self-like**, transação RMW em `memberStats` (deriva `level` de `gamificationLevelForPoints` p/ pontos/nível **nunca divergirem**, clamp em 0), append em `pointsEvents`; triggers `onCommunityLikeCreated` (+1) / `onCommunityLikeDeleted` (-1) sobre `communityPosts/{postId}/likes/{likerId}`. Regras Firestore (likes subcollection + memberStats/pointsEvents/leaderboards) + 7 testes de anti-cheat. *Double-fire de entrega at-least-once é tradeoff aceito de MVP (engajamento, não dinheiro).*
  - **F2 `f4e608a`** — leaderboards: cron `rebuildLeaderboards` (`onSchedule every 6 hours`, UTC); all-time = `memberStats` orderBy points desc limit 20; 7d/30d = soma `pointsEvents.delta` na janela por uid, join `displayName` + nível all-time **atual**. (`Date.now()` é válido no runtime de Cloud Functions — a restrição de workflow-script não se aplica aqui.)
  - **F3 `635c8da`** — UI: `src/lib/data/gamification.ts` (data layer client: `setCommunityPostLike`, `subscribeToPostLikes`/`MemberStatsMap`/`Leaderboard`); `level-badge.tsx` (pílula "Lv N", clamp 1..9); `community-leaderboard.tsx` (tab, toggle 7d/30d/all-time, highlight do usuário atual + footer "your standing" se fora do top-20, **keyed-state** p/ evitar `set-state-in-effect`); `course-community-feed.tsx` (tab Leaderboard, listener `memberStats` no feed, botão de like Heart com contagem **desabilitado no próprio post** + dica "Likes on your posts become points.", badges em autores de post **e** comment).
- **Verificação:** tsc FE (`tsc --noEmit -p tsconfig.json`) EXIT 0; tsc functions (`npm run lint`) EXIT 0; eslint dos 4 arquivos da Phase 3 EXIT 0 (refatorei o map de comentários p/ block-body, eliminando `!` non-null assertion); **42/42** rules tests (`npm run test:rules` — firestore 32 + storage 10; os 7 testes C1 verdes, `PERMISSION_DENIED` no stderr é o resultado esperado dos casos negativos). Deploy `deploy:rules` (Phase 1) + `deploy:app` EXIT 0 — `onCommunityLikeCreated`/`onCommunityLikeDeleted`/`rebuildLeaderboards` "Successful update operation", hosting "release complete". *Verificação E2E LIVE da UI N/A* (feed é auth+matrícula-gated; fora de política sem credenciais) — teto = build/deploy verdes + 42 rules tests provando o anti-cheat no engine de regras + functions live.
- **Nota de escala (observada nos rules tests, No-Invention):** alguns paths de `update` tocaram o teto de "1000 expressions to evaluate" do engine Firestore (L1439/L1786) durante casos negativos — **sem impacto funcional** (o write era negado de qualquer forma; os 42 testes passaram, todos os positivos inclusos), mas é item-vigia conforme as regras crescem.
- **Próximo (C4, depende do C1):** gating por nível ("unlock chat/post/curso ao subir") é a fase 2 — o "dente" de retenção que amarra gamificação→monetização (score 12).

### A1-cont — registro técnico (2026-06-07) — contraste AA dos foregrounds semânticos no dark mode

- **Premissa verificada (No-Invention):** o subset A1 já corrigira os fundos/superfícies do dark, mas a auditoria (workflow 8-agentes) achou a causa-raiz remanescente: `[data-theme="dark"]` em `globals.css` **remapeia superfícies** porém **nunca** sobrescreve os **tokens de cor de FOREGROUND** (`--color-accent #b22234`, `--color-success #1f8a5b`, `--color-warning`, `--color-danger`). Logo todo TEXTO/ÍCONE vermelho/verde herdava a cor saturada de light sobre fundo escuro → medição **~2.73:1** (accent) e similar p/ success, **reprovando** WCAG AA (4.5:1 p/ texto normal; um caso 12px-bold conta como normal, não large).
- **Estratégia `-fg` (fail-safe, escolhida vs. o inverso `-solid`):** criados `--color-accent-fg` e `--color-success-fg`. No **`:root` valem exatamente o token base** (`#b22234` / `#1f8a5b`) → migrar **qualquer** uso é **no-op no light** (garantia de zero regressão, não "deveria"). Sob `[data-theme="dark"]` divergem p/ tons claros: **accent-fg `#e36b78`**, **success-fg `#5fc99a`**. Os tokens **base** continuam saturados p/ fills sólidos (white-on-fill): badge de notificação, hover de sidebar, group-hover de CTA, accent de form-control. **Por que fail-safe:** rotear foreground p/ `-fg` e deixar fills no base significa que **um ponto de texto que o script perca fica no status quo (nunca regride white-on-fill)** — o oposto (`-solid` p/ fills) falharia inseguro.
- **Ratios medidos (cálculo WCAG sRGB próprio, rampa de superfícies escuras `#0a0f1a < #0f1626 < #141d33 < #1c2841`):**
  - accent texto **antes** ~**2.73:1** (reprova) → **depois** accent-fg `#e36b78`: **4.646** (surface-strong `#1c2841`, pior caso opaco) / 5.298 (soft) / 5.709 (surface) / **6.058** (base). Todos ≥ 4.5. ✓
  - success texto **antes** baixo (similar) → **depois** success-fg `#5fc99a`: **7.2→9.4:1** ao longo da rampa. ✓
- **Blocker pego pelo próprio Verify (valor do adversarial-verify):** o Design propôs accent-fg `#e06472`, que mede **4.363:1** em surface-strong — **reprova** p/ o caso 12px-bold (delta negativo em `teacher-overview-metrics.tsx`). Corrigido p/ `#e36b78` (**4.646** no mesmo fundo, reconferido à mão). O workflow flagou o próprio design — exatamente o ponto.
- **260 migrações fail-safe** (script idempotente por **prefixo de foreground** — `text|border-[tblrxy]|border|decoration|ring|outline|fill|stroke|caret|placeholder`-`[var(--color-accent|success)]` → `-fg`; **nunca** casa `bg-`/`accent-`/`from-`/`to-`/`via-`/`group-hover:bg-`/`text-white`):
  - **16 regras** em `globals.css` (`color:`/`border:`/`border-color:`/`border-left:`/`border-bottom-color:` var(--color-accent) → -fg; os 4 `background: var(--color-accent)` **intocados**).
  - **244 utilitários** em **94 arquivos** `.tsx` (241 accent + 3 success), abrangendo `src/app/*/page.tsx` e `src/components/{account,admin,auth,certificates,courses,help,instructors,learn,platform,shared,site,support,teacher}/*.tsx`.
- **No-Invention catch (que o workflow tinha errado):** li `globals.css` direto e achei que o modal de lição tem bg `linear-gradient(#ffffff → var(--color-surface-soft))` — **bolsão claro-no-escuro** (o topo continua branco no dark). Texto accent ali **deve** ficar `#b22234` (vermelho escuro), **não** clarear p/ `-fg` (reprovaria sobre branco). 4 linhas (modal) **excluídas** da migração via `MODAL_EXCLUDE` e **deferidas (A1-cont-2)**.
- **Segurança da migração `.tsx`:** os overrides Tailwind globais (`[data-theme="dark"] .bg-white { background-color: var(--color-surface) !important }` e `.button-outline { background: var(--color-surface) }`) garantem que superfícies `bg-white`/`button-outline` nos componentes **invertem** no dark → migrar o texto accent nelas p/ `-fg` é seguro (contraste calculado contra a superfície invertida, não contra branco).
- **DEFERIDO → A1-cont-2:** (1) ~12 superfícies **white-on-`--color-primary`** hardcoded (`globals.css` ~1013/1017/1235/1721/2007/2235/3204/3210/3225/3554/3779) — bug **separado** de inversão de `--color-primary`, **não** regredido pela abordagem `-fg` (tokens base/fills intocados); precisa de tinta escura `var(--color-base)` sobre primary invertido **ou** tokens fixos de sidebar-active. (2) Theming dark holístico do **modal de lição** (o gradiente branco→escuro).
- **Verificação:** `tsc --noEmit -p tsconfig.json` EXIT 0; `npm run build` (root: tsc+lint+CSS) EXIT 0; deploy `deploy:app` EXIT 0 (hosting "release complete"). Build-artifact (`.next/static/chunks/*.css`): `#e36b78`×1, `#5fc99a`×2, `--color-accent-fg`×30, base `#b22234`×60 (**fills preservados**). **LIVE (prod, WebFetch do chunk servido):** `[data-theme=dark]{…--color-accent-fg:#e36b78}` + `--color-success-fg:#5fc99a` + `.status-chip--danger{color:var(--color-accent-fg)}` confirmados. *Auditoria visual pixel-a-pixel LIVE N/A por política* (sem credenciais p/ navegar autenticado) — teto = cálculo WCAG verificado + build/deploy verdes + tokens confirmados no bundle servido. Commit `489e293`.

### A1-cont-2 — registro técnico (2026-06-07) — white-on-primary + dark theme do modal de lição

- **Premissa verificada (No-Invention):** auditei direto `globals.css`, não confiando nos números de linha do A1-cont (deslocaram após as edições). Confirmado: `--color-primary` = `#1a365d` (navy) no `:root` → inverte p/ `#8fb4e4` (azul claro) no `[data-theme="dark"]`; `-dark` → `#d9e8fb` (quase branco); `-light` → `#b8d2f0`. Logo **texto/ícone branco sobre primary** vira branco-no-claro no dark, e **texto que usa tokens invertidos** (primary/ink/ink-soft) sobre **bolsão branco** vira claro-no-claro. Mapeei as 13 regras `background: var(--color-primary*)` e triei cada uma.
- **Parte 1 — 4 falhas white-on-primary (texto/ícone):**
  - `.account-settings-tab--active span:last-child span:last-child` e `.course-upload-preset--active …` — sub-labels `rgba(255,255,255,0.76)`; o **label pai já** usa `var(--color-base)` (auto-adapta), o sub-label não → dark-scoped p/ `var(--color-base)`.
  - `.create-course-payment.is-active > span` — `#ffffff` no indicador de método de pagamento ativo.
  - `.learner-continue-card__icon` — `#ffffff` no tile de ícone.
  - Todos dark-scoped p/ `var(--color-base)` = **8.8:1** sobre o primary invertido (`#8fb4e4`, L≈0.441; base dark `#0a0f1a` L≈0.006). Precedente idêntico ao `.button-solid` (318-323), chevron (1752) e `.dash-card--accent` (3393). `--color-ink-muted` dark (`#6b7e9a`) **não** serve aqui (1.93:1 sobre primary — é p/ superfícies escuras), por isso base sólido.
  - **Descartados com verificação:** toggles `.settings-toggle--on`/`.lesson-modal-setting button.is-on` (estado por **posição** do knob, não cor) e `.member-classroom-hero__cover` (bg primary fica **atrás do `img`**; o texto vive em `.__coverOverlay` com bg próprio `rgba(15,39,68,0.78)` escuro). Nenhuma é falha de texto. `::selection`/scrollbar dark já têm tratamento próprio.
- **Parte 2 — dark theme do modal de lição (falha de AA real):** o `.lesson-modal` tem bg `linear-gradient(180deg, #ffffff 0%, var(--color-surface-soft) 100%)` — no dark o **topo continua branco** (#ffffff hardcoded), o fundo escurece. O header (`rgba(255,255,255,0.76)`), tabs/footer/close/choice/upload/assets/setting (#ffffff) ficam **brancos**; os textos (h3 `var(--color-primary)`→azul-claro, p `var(--color-ink-soft)`→slate-claro) invertem → **h3 ~1.9:1, corpo ~2.0:1** sobre branco. **É AA real, não drift estético.** Re-tematização (1 bloco consolidado, 13 overrides dark-scoped):
  - `.lesson-modal` → gradiente escuro `surface → base` (camada mais escura) + radial accent-fg; `border-color` → `line-strong`.
  - header/tabs/footer → `rgba(15,22,38,0.76–0.92)` (surface translúcida sobre o body); close → `surface-strong`.
  - cards de conteúdo (choice, upload__trigger, inline-status, upload, assets article, setting) → `surface-strong` (**levantam** do body mais escuro).
  - `.lesson-modal-video` (gradiente `primary-dark→primary`, que inverte p/ claro com texto branco) → gradiente escuro `surface-strong→surface` — **1 override conserta o card + todos os filhos brancos** (texto/chips) de uma vez.
  - knob do on-toggle (`button.is-on::after`) → `var(--color-base)` (tinta escura, visível 8.8:1 no track azul-claro; a troca de cor reforça o estado posicional).
  - accent/success internos → `-fg`: crumb/eyebrow (2330), tab-active `border-bottom-color` (2402), upload `__error` (2652), `__success` (2658). Como `-fg` = base no light, light **não muda**; no dark (modal agora escuro) leem certo.
  - Superfícies que já usavam `var(--color-surface-soft)` (tabs span, note, empty, upload__file, assets small) e os `input/select/textarea` (cobertos pelo override global `[data-theme="dark"] input`) **auto-escurecem** — sem override extra.
- **Sanidade:** re-grep do range 2293-2790 confirmou que as 13 `background:#ffffff`/`rgba(255,255,255)` remanescentes são todas (a) sobrescritas pelos overrides dark, (b) o knob "off" intencional (branco no track escuro), (c) o chip translúcido do video-card (agora escuro), ou (d) inputs (override global). Zero bolsão branco órfão.
- **Zero regressão no light:** todos os fixes são `[data-theme="dark"]`-scoped ou tokens `-fg` (= base no light). Light mode **byte-idêntico**.
- **Verificação:** `npm run build` EXIT 0 ("✓ Compiled successfully in 12.1s"); `deploy:app` EXIT 0 (functions **Skipped** "No changes detected" — só toquei CSS; SSR "Successful update operation"; hosting "release complete"). Os **6 marcadores** A1-cont-2 (`[data-theme=dark] .lesson-modal{…surface→base}`, crumb `-fg`, learner-icon, payment span, acct-tab sublabel, modal-setting knob) confirmados por grep no **artefato deployado** (`.firebase/skillsetusaofficial/hosting/_next/static/chunks/0a4r_7xbq_fei.css`, byte-idêntico ao upload), e o `index.html` live referencia exatamente esse chunk **content-hashed**. **Garantia forte:** nome de chunk Next = hash do conteúdo → se o HTML live aponta `0a4r_7xbq_fei.css`, o arquivo servido é **byte-idêntico** ao staged (senão teria outro nome). *O WebFetch do CSS live deu "NO" por **truncamento** — o arquivo tem 133KB minificados e o `account-menu #e36b78` (achado, 57% do arquivo) precede as regras A1-cont-2 (74-79%, na cauda truncada); não é falha de deploy nem lag de CDN.* Teto de verificação = artefato deployado provado por grep (6/6 marcadores) + HTML-reference content-hashed + release complete. Commit `7aabb9a`.

### H2 — registro técnico da correção (2026-06-07)

- **Dividido (como H1/C5):** o item pedia "validUpload hardening / cross-check com asset-doc". Auditoria separou um **fix real e seguro** (bloquear markup executável) de uma parte **gated por arquitetura** (cross-check exige re-arquitetura). Shippei o primeiro, surfacei o segundo.
- **Fix shippado — bloquear `text/html`:** `storage.rules` linha do `text/.*` virou `matches('text/.*') && !matches('text/html.*')`. Racional idêntico ao SVG já barrado (`!= 'image/svg+xml'`, `:55`): markup que o browser executa não deve ser armazenado como asset. Vetor concreto: `getProtectedCourseAssetObjectUrl` (`course-assets.ts:156-159`) materializa o asset via `URL.createObjectURL(blob)`; um `text/html` aberto/navegado roda script na origem do viewer (aluno matriculado) → exfiltração de sessão. `text/html.*` (negado) também pega `text/html; charset=utf-8`. Zero falso-positivo: material legítimo é `text/plain`/`text/markdown`/`text/csv` (`course-asset.ts:60-90,113-119`), nunca html.
- **Por que o cross-check vs asset-doc NÃO foi feito (verificação que evitou regressão):** `uploadCourseAsset` (`course-assets.ts`) faz `uploadBytesResumable` (`:85`) e só **depois** do sucesso `setDoc(assetDoc, …)` (`:125`). A regra de Storage write (`storage.rules:104-107`) roda **no momento do upload do blob** — quando o doc ainda não existe. Adicionar `firestore.exists(.../assets/$(assetId))` rejeitaria **100% dos uploads** = regressão total. O fix correto exige fluxo **doc-first** (criar doc "pending" antes, GC de orphans, esconder pending do render) — decisão de arquitetura, não tweak de regra. Surfaçado como `task_e346eb48` (H2-cont) com 3 opções (doc-first / server-mediated signed-URL / aceitar risco residual documentado).
- **Risco residual aceito no slice:** blobs órfãos (sem asset-doc) ficam limitados a **professor que possui o curso, escrevendo sob seu próprio path** (`ownerId == uid` + `courseIsEditableByOwner`, `:105-106`) — não é abuso anônimo; e o client já deleta o blob em falha de `setDoc` (`course-assets.ts:148-151`). Sev baixa (score 6), coerente.
- **Verificação:** **32/32** rules tests no emulador Firebase (`firestore-rules` 22 + `storage-rules` 10, +2 novos), rodando contra o **arquivo `storage.rules` exato** que foi deployado; `firebase deploy --only storage` reportou "rules file storage.rules compiled successfully" + "released rules to firebase.storage". *Teste de upload autenticado LIVE não aplicável* (exige credenciais, fora de política) — o teto de verificação é emulador (o próprio engine de regras provou o comportamento) + compile + release, todos verdes. Commit `dae7d78`.

### H2-cont — registro técnico da DECISÃO (2026-06-07) — re-arquitetura de upload (doc-first / signed-URL)

- **O que era:** o fix *literal* do backlog (`validUpload` cross-check vs asset-doc) **não é drop-in** — `uploadCourseAsset` grava o blob **antes** do doc Firestore (`course-assets.ts:85` antes de `:125`); exigir doc pré-existente na regra de Storage rejeitaria **100%** dos uploads. Chip `task_e346eb48` pedia decisão entre 3 opções (A doc-first / B signed-URL server-mediated / C aceitar residual). Por ser money-adjacent (asset = conteúdo gated, vendido), pesquisei o mercado antes de agir (mandato).
- **Pesquisa de mercado (fontes abaixo):**
  - **Padrão ouro = signed URL emitido pelo servidor** (direct-to-cloud): cliente pede permissão → backend valida (ownership, quota, tipo) + cria o registro + emite URL assinada time-limited → cliente sobe direto p/ o storage; backend faz pós-processamento. Tira o servidor do caminho de dados, centraliza o gate. (S3 presigned POST / GCS signed URL — usado por plataformas de curso/LMS.)
  - **Limpeza de órfãos = lifecycle/cron** ("arquivos num dropbox além de X tempo são deletados automaticamente").
  - **Quota por usuário** (total storage, eviction/notificação no threshold) é padrão.
  - **Firebase oficial:** Storage Rules **podem** cross-referenciar Firestore via `firestore.get/exists`, MAS (1) contam p/ quota/billing de Firestore e (2) **máx 2 docs por avaliação**. A regra de write **já** lê `courses/{courseId}` (via `courseIsEditableByOwner`) — adicionar `exists(asset-doc)` fica **no limite de 2** e cobra um read por upload.
- **Auditoria de código (No-Invention):**
  - `validUpload` (`storage.rules:48-83`): após a fatia H2, o lado de **conteúdo está sólido** — `image/*` exceto svg, `text/*` exceto html, `application/octet-stream` **gated por allowlist de extensão** (`validLessonMaterialFallback:44-46` → `pdf|txt|md|csv|doc(x)|ppt(x)|xls(x)|zip|mp3|m4a|wav|ogg`). O octet-stream **não** é buraco largo. O vetor de markup-ativo (html/svg) — único que executa script via `getBlob`→`createObjectURL` — **já está bloqueado**.
  - Write rule (`:112-115`): `signedIn && ownerId==uid && courseIsEditableByOwner && validUpload`. Não é vetor anônimo nem cross-tenant — é **dono-do-curso escrevendo sob o próprio path** de um curso editável dele.
  - Cleanup client-side **já existe**: `uploadCourseAsset` deleta o blob no `catch` (`course-assets.ts:148-151`). Órfão só surge num crash de ~ms entre upload-success e `setDoc` → **frequência ~zero**.
- **Decisão: Opção C — aceitar o residual documentado; signed-URL é o upgrade-path.** Racional (proporcionalidade de eng. sênior):
  - O **vetor real de segurança já está fechado** (fatia H2). O residual H2-cont é **integridade/órfão**, severidade **baixa** (score 6), impacto concreto **trivial** (alguns blobs órfãos de uploads abortados, e o cliente já faz GC na falha).
  - **Re-arquitetar o fluxo de upload funcionando** (doc-first: +read de Firestore por upload, cap de 2 docs, GC de docs `pending`, filtro de render, mudança no type) introduz **risco de regressão num core money-adjacent** por ganho marginal — desproporcional.
  - **Signed-URL (B)** é o ideal de mercado, mas é a **maior** mudança; só se justifica quando surgir necessidade de produto de **quota / malware-scan / entrega via CDN** — e aí ele **subsume** a integridade doc-first. Fica recomendado como story futura deliberada, não side-effect autônomo.
  - **Gatilho de upgrade documentado:** ao precisar de (a) enforcement de quota de storage por professor, (b) scan de malware no upload, ou (c) entrega via CDN/signed-download → implementar o pattern signed-URL (Cloud Function emite a sessão de upload + cria o doc atomicamente; regra de Storage passa a `deny` de write direto do cliente).
- **Espelha o desfecho do H1:** investigação honesta cujo resultado correto é "sem mudança arriscada", com o gatilho/condição documentado. Sem código, sem deploy. *(A fatia H2 — bloqueio de `text/html` — já shippou o ganho de segurança real, commit `dae7d78`.)*

### H1 — registro técnico da verificação (2026-06-07)

- **Tipo de item:** verificação de segurança ("verificar se é intencional"), não um defeito conhecido. Resolução honesta possível = "intencional, sem mudança" (como T2). Forçar um fix aqui seria **regressão**.
- **Regra auditada (`firestore.rules:1442-1450`):** `allow read: if resource.data.status in ['published','in_review'] || (signedIn() && hasEnrollmentForCourseSlug) || (signedIn() && ownerId==uid) || isAdmin()`. A 1ª cláusula é a world-readable (sem auth) p/ os 2 status públicos.
- **Intenção documentada (1443-1446):** sell-on-submit — "published AND in_review are publicly readable so a just-submitted course is immediately visible + purchasable. Approval is non-blocking; draft / needs_changes / inactive stay private to the owner and admins." Bate com `published-courses.ts:24-31` (query pública `status in ['published','in_review']`) e com o recap FASE 0 #1.
- **Análise de ciclo de vida (in_review é alcançável sem intenção?):** `create` exige `status=='draft'` (`:1455`); a ÚNICA transição p/ `in_review` é `teacherCanSubmitOwnedCourseForReview()` (`:1462`) = submit explícito do professor. Sob sell-on-submit, submit **é** publicar. in_review→published/needs_changes via ops/admin (`opsCanUpdateCourseStatus`). Rejeição→`needs_changes` (privado de novo); unpublish→`inactive` (privado). ⇒ **não há "submeter privado"; não há estado unlisted.**
- **Análise de superfície de exposição:** o read world-readable do **doc do curso** expõe só metadata de catálogo/marketing (título, summary, módulos/títulos de lição, preço, rating, ownerId, outcomes). **Conteúdo protegido NÃO vaza** — gated por ownership/enrollment independentemente do status: `assets` (`:1480-1482` admin/owner/enrolled), `lessonComments` (`:1501-1503`), `communityPosts`+`comments` (`:1568-1569,1585-1590`), `enrollments` (`:1541-1542`), `enrollments/progress` (`:1553`). `courseReviews` (`:1521-1526`) é public p/ published **e** in_review — coerente (prova social de curso público), e escrito só via Cloud Function (`create/update/delete:false`).
- **Por que `published` e `in_review` idênticos não é leak:** ambos são estados **públicos por design**; in_review não expõe nada além do que published já expõe. Smoke-courses (`smoke-` prefix) ficam published+reachable por URL mas escondidos do browse por **filtro client** (`course-marketplace.tsx:32-34,101`) — carve-out intencional, não falha de regra.
- **Veredito:** intencional, documentado, consistente e corretamente escopado. O tradeoff deliberado (curso vai a público+comprável no submit, antes de review humano) é decisão de produto já firmada — **não** um bug de segurança. Nenhuma mudança de regra. Nada a deployar.

### C5 — registro técnico da correção (2026-06-07)

- **Premissa dividida (como A1):** o item pedia "trending/featured/sort UI". Leitura direta do código separou um **slice limpo e não-gated** (sort UI sobre dado existente) de duas partes **genuinamente gated** (trending precisa de dado que não existe; featured é decisão de produto). Shippei o slice, surfacei o resto — mesmo padrão de A1 (subset + A1-cont).
- **Estado pré-fix:** `course-marketplace.tsx` tinha chips de categoria + busca, **nenhum sort**; `visibleCourses` só filtrava, preservando a ordem do merge `[...courses, ...publishedCourses]`. `courses` (demo estático) **default `[]`** na página `/courses` → o marketplace live é 100% teacher courses, ordenados A→Z por título em `published-courses.ts:43`.
- **Dado verificado disponível (No-Invention):** `TeacherCourse` tem `ratingAverage?: number`, `ratingCount?: number`, `priceAmountMinor?: number | null` (todos bem-tipados). `createdAt`/`updatedAt` são `unknown` (Firestore Timestamp, não confiavelmente populado) → **NÃO** inventei sort "Newest" sobre shape não-verificado.
- **Fix:** (1) `catalog.ts` — `CourseCard` ganha `priceAmountMinor?`/`ratingAverage?`/`ratingCount?` (opcionais → aditivo, não-breaking). (2) `published-courses.ts` — `teacherCourseToCourseCard` popula os 3 (strings de display continuam fonte da verdade p/ render; numéricos só alimentam comparadores). (3) `course-sort.ts` (novo) — `CourseSortKey`, `courseSortOptions`, `sortCourseCards` puro: rating desc (não-avaliado por último), price asc/desc (sem-preço afunda nas **2** direções, p/ o catálogo precificado liderar), título como tiebreaker final em todo modo. (4) `course-marketplace.tsx` — estado `sortKey` (default `"alpha"`, **não** sincronizado na URL — é preferência de view, não filtro compartilhável como cat/q), `<select>` de Sort empilhado sob a busca, render passa a `sortCourseCards(visibleCourses, sortKey)`. (5) `courses/page.tsx` — skeleton SSR ganhou o 2º stub de controle p/ espelhar o Sort (o comentário do skeleton diz que ele espelha o loading real).
- **Zero regressão por design:** default `"alpha"` reproduz exatamente o A→Z histórico; quem não toca no controle não vê mudança. Campos novos do card são opcionais → demais consumidores intactos. Select usa `bg-white` **igual** ao input de busca irmão (não introduz inconsistência nova; theming dark de form-controls é território A-cluster, fora do escopo C5).
- **DEFERIDO → `task_b9fb7f79` (C5-cont):** "trending" exige agregação de atividade (counter rolling de enrollments por janela 7/30d via Cloud Function + campo de score) — dado inexistente hoje; "featured" exige decisão de curadoria (flag editorial `featured` vs algorítmico). Ambos surfaçados como chip p/ decisão do fundador, não auto-executados.
- **Verificação:** `next build` EXIT=0 (tsc+lint+CSS) · **122/122** vitest (24 arquivos; +7 testes de `course-sort` cobrindo cada edge: default alpha, A→Z, rating-desc+unrated-last, tiebreak por count/título, price-asc com sem-preço-last, price-desc **ainda** com sem-preço-last, pureza/não-mutação) · deploy EXIT=0 (SSR atualizada, demais Skipped). **LIVE (prod):** chunk local→prod `0zh7kk9c-jkgr.js` (status 200) contém `price-asc`/`price-desc`/`Top rated`/`Price: low to high`/`Alphabetical`; `/courses` serve 200. Commit `04e03b2`.

### C5-cont (Fase 1) — registro técnico (2026-06-07) — featured editorial ops-gated

- **Pesquisa de mercado antes de decidir (mandato p/ decisões delicadas):** Coursera/Udemy/Skool/Teachable apresentam um **default curado** ("featured"/"top picks") MAS, quando o usuário escolhe um sort explícito (preço, avaliação), o pin editorial **cede** ao sort — pinar um curso featured acima de um sort de preço explícito lê como "sort quebrado". Daí a decisão de design.
- **Decisão de design — "featured" é um MODO de sort, não um pin cross-sort:** adicionei `CourseSortKey "featured"` ("Featured & top picks") como **primeira opção e novo default** do marketplace; os sorts de dado (rating/price) ficam **puros**. Vantagens: (1) curadoria editorial tem uma casa clara (a view default); (2) sort explícito permanece honesto; (3) **zero regressão** — enquanto nenhum curso está featured, "featured" = featured-slice(vazio)+A→Z = **byte-idêntico** ao A→Z que era o default. Alternativa rejeitada: pinar featured em **todos** os sorts (furaria um sort de preço explícito → surpreendente).
- **No-Invention / dado verificado:** `featured?: boolean` + `featuredRank?: number | null` adicionados a `TeacherCourse` (`teacher-course.ts:108`) e `CourseCard` (`catalog.ts:32`) como **opcionais** (aditivo, não-breaking; cards demo/estáticos omitem → tratados como não-featured). Mapper `teacherCourseToCourseCard` popula `featured ?? false`/`featuredRank ?? null` (`published-courses.ts:121`).
- **Sort (`course-sort.ts`, puro/total):** modo "featured" = `sortFeaturedFirst` — featured por `featuredRank` asc (rank ausente afunda p/ `MAX_SAFE_INTEGER`), título como tiebreaker; não-featured seguem A→Z. Demais modos inalterados. Marketplace default `sortKey` `"alpha"`→`"featured"` (`course-marketplace.tsx:54`).
- **Escrita ops (`teacher-courses.ts`):** `setCourseFeatured(courseId, featured, featuredRank=null)` grava **exatamente** `featured`+`featuredRank`+`updatedAt` (casa `courseChangedOnly(['featured','featuredRank','updatedAt'])`); `featured=false` zera o rank (nunca carrega placement stale).
- **Segurança (firestore.rules):** `opsCanUpdateCourseFeatured()` — `isOps()` (role `ops` **ou** admin) + diff scoped + `featured is bool` + `featuredRank` null-ou-int-0..1M; **avaliado ANTES de `validCourseShape()`** (mesmo padrão dos outros predicados scoped — o validador de ~26 campos estoura o orçamento de 1000 expressões no path de update). `'featured'`/`'featuredRank'` adicionados ao `hasOnlyAllowedCourseKeys()` (senão um teacher-save num curso featured reprovaria). **Professor não auto-featura:** `teacherCanSaveOwnedCourse` usa `courseChangedOnly([...])` cuja lista **exclui** `featured`/`featuredRank` → qualquer write de teacher tocando-os falha o `hasOnly()`. Provado em emulador.
- **UI ops (`managed-course-panel.tsx`):** botão Feature/Unfeature (via `setCourseFeatured`) + badge "Featured" — só em cursos `published` (featured num `inactive` não apareceria no marketplace, cuja query é `status in ['published','in_review']`).
- **Verificação:** `next build` EXIT=0 (tsc+lint+CSS) · **128/128** vitest (+6 testes featured de `course-sort`: featured=alpha quando nada featured / pin acima do resto / ordem por rank+título / tiebreak por título / sorts explícitos puros / pureza — total 13 no arquivo) · **48/48** rules tests `npm run test:rules` (firestore 38 + storage 10; +6 featured: ops+admin **podem**, teacher **não** auto-featura, student **não**, smuggle de `priceAmountMinor` bloqueado, `featuredRank` malformado "1"/-5 rejeitado) · `deploy:rules` EXIT=0 (**firestore.rules released**; storage já up-to-date) · `deploy:app` EXIT=0 (SSR `ssrskillsetusaofficial` atualizada pois o build do marketplace mudou; 28 functions de negócio Skipped; hosting released). **Ordem de deploy:** rules **antes** do app (senão um clique "Feature" antes das rules daria PERMISSION_DENIED).
- **DEFERIDO (Fase 2 — trending):** `onEnrollmentCreated` (Firestore trigger em `enrollments/{id}`, espelhando `onCommunityLikeCreated`) que incrementa `enrollmentCount` lifetime; cron `rebuildTrending` (janela 7d → `trendingScore` por curso); campos server-only no allowlist; sort key "Trending"; surfacing no marketplace. **→ ENTREGUE na Fase 2 (commit `52de8db` é a Fase 1; ver registro da Fase 2 abaixo).**

### C5-cont (Fase 2) — registro técnico (2026-06-07) — trending now (enrollment-velocity)

- **Fecha o thread C5.** A Fase 1 (featured) era decisão de curadoria; esta (trending) era o que estava deferido por **falta de dado de atividade** ("trending precisa de agregação de métrica — enrollments por janela — que não existe"). Construí o dado.
- **Definição (mercado):** trending = **número de matrículas nos últimos 7 dias** por curso. É o sinal de popularidade mais simples e honesto, e espelha o que o benchmark Skool faz ("ordena por membros/atividade", `skool-vs-skillset-2026-06-06.md`). Sem invenção de fórmula de decay/peso — count-na-janela é explicável e auditável.
- **Onde o dado vive (decisão de arquitetura):** `trendingScore` **no próprio course doc** (server-only, como `ratingAverage`), **não** numa coleção separada `trendingCourses/{window}` (que o leaderboard usa). Razão: o marketplace **já** assina os course docs (`subscribeToPublishedTeacherCourses`), então pôr o score no doc faz o sort virar um **comparador puro** sobre `CourseCard` — idêntico ao padrão de rating/price/featured. Coleção separada exigiria 2ª subscription + join. Mesma lógica do featured.
- **Idempotência por design (recount > delta):** o cron `rebuildTrending` (6/6h, espelha `rebuildLeaderboards`) **recomputa** o score de cada curso da janela de 7d **direto da coleção `enrollments`** (`where createdAt >= cutoff`, agrupa por `courseId`). Como é recount, é **naturalmente idempotente e self-healing** — um trigger perdido OU duplicado **nunca** corrompe o ranking; a próxima passada recomputa da fonte. Itera **todo** curso listável (`status in [published, in_review, inactive]`), não só os com atividade, p/ **zerar** quem saiu da janela (com guarda `trendingWriteNeeded` que pula no-op writes → sem churn de snapshot p/ os listeners do marketplace).
- **Trigger `onEnrollmentCreated`** (`enrollments/{id}`, espelha `onCommunityLikeCreated`): mantém **só** o `enrollmentCount` lifetime via `FieldValue.increment(1)` no course doc, dentro de `try/catch` que **nunca** deixa um erro de counter vazar p/ o write de matrícula (money-path intocado). Re-ativações de assinatura são **updates**, não creates → não disparam o trigger → não contam duplo (semântica correta de "matrículas únicas"). Posture at-least-once **assumida e documentada** (mesma do counter de likes do C1); é social-proof não-financeiro, e o **score** (que dirige a feature) é imune a drift porque é recomputado pelo cron.
- **No-Invention / verificado:** mapeei os **4 caminhos de criação de enrollment** (free `createFreeCourseEnrollment` `index.ts:2051`; one-time `handleCheckoutCompleted` `:3662`; subscription `handleCourseSubscriptionInvoicePaid` `:3878`; admin pré-posicionado) — **nenhum chokepoint único** → trigger no nível da coleção é a escolha correta. Todos setam `courseId`. Lógica pura (agregação/score/skip-write) extraída p/ `functions/src/course-trending.ts` (estilo `course-analytics.ts`), testada isolada do emulador.
- **Segurança (firestore.rules):** `enrollmentCount`/`trendingScore` adicionados ao `hasOnlyAllowedCourseKeys()` (senão um teacher-save num curso já scored reprovaria) + validados em `validCourseShape` (`enrollmentCount` int 0..1e8; `trendingScore` number 0..1e8), **fora** de todo `courseChangedOnly` → **só Admin SDK** (trigger+cron) escreve. Provado em emulador: teacher write de `trendingScore` e ops write de `enrollmentCount` **falham** (49º rules test). O create-path (que de fato roda `validCourseShape`) segue passando com os 2 campos novos → orçamento de 1000 expressões OK.
- **Sort (`course-sort.ts`):** `"trending"` vira **2ª opção** ("Trending now", depois de Featured) — **sort explícito**, **não** default e **não** pina featured (coerente com a Fase 1: sort explícito = exatamente aquilo). Comparador: `trendingScore` desc → `enrollmentCount` desc (tiebreak lifetime) → título. **Sem atividade → colapsa p/ A→Z** (no-op gracioso até o 1º cron popular scores). `CourseCard`/`TeacherCourse`/mapper ganham os 2 campos opcionais (`?? 0`).
- **Verificação:** functions `tsc` EXIT=0 · `next build` EXIT=0 · **143/143** vitest (+10 de `course-trending`: courseId-extract, agregação, resolve-score, write-needed; +5 de `course-sort` trending: opção presente, score-desc, tiebreak por enrollmentCount/título, colapso A→Z, pureza) · **49/49** rules tests (+1 server-only trending) · eslint limpo · `deploy:rules` EXIT=0 (firestore released) + `deploy:app` EXIT=0 (**`onEnrollmentCreated` `Successful create` + `rebuildTrending` `Successful create`** — 2 functions novas live; SSR atualizada; hosting released). **Nota de cadência:** o 1º `trendingScore` aparece após a 1ª execução do cron (≤6h) ou trigger manual; até lá "Trending now" = A→Z (gracioso). Commit a seguir.
- **C5 ENCERRADO:** Fase 1 (featured) + Fase 2 (trending) entregues. O item C5 (score 12) está completo: sort UI (slice original) + featured editorial + trending por velocidade.

### P2 — registro técnico da correção (2026-06-07)

- **Premissa parcialmente stale (honestidade epistêmica):** o backlog pedia "swap instantâneo → cross-fade/rotate" no `theme-toggle.tsx`. Leitura direta: o ícone **já** cross-fadeia e rotaciona — Sol/Moon `absolute`, `transition-all duration-300 ease-out`, `rotate/scale/opacity` por tema, `motion-reduce:transition-none`. O literal já estava entregue (como T2). O **defeito perceptível real** é outro: o `theme-provider` aplica o tema via `document.documentElement.dataset.theme = resolvedTheme` (`:65`) — swap **instantâneo** do atributo, sem transição global; logo as cores da página inteira (fundos, texto, bordas, sombras) dão snap duro enquanto só o ícone anima. Esse snap **é** o "swap instantâneo" que o fundador percebeu.
- **Fix (escopo cirúrgico, 2 arquivos):** (1) `theme-provider.tsx` — `setMode` adiciona `.theme-transitioning` no `<html>` **antes** do efeito de `resolvedTheme` repintar, com `useRef` de timeout que cancela o anterior em toggles rápidos e remove a classe após 340ms. Adicionada **só em `setMode` (ação do usuário)** → o mount inicial (que aplica o tema armazenado via efeito, sem `setMode`) **não** pisca transição. (2) `globals.css` — regra `html.theme-transitioning *,::before,::after { transition: background-color/border-color/color/fill/stroke/box-shadow/opacity/transform 300ms ease !important }` dentro de `@media (prefers-reduced-motion: no-preference)`.
- **Por que `opacity`+`transform` entram na lista:** a regra global usa `transition: … !important` em `*`, o que **substituiria** o `transition-all` do ícone durante a janela do toggle — exatamente quando o ícone anima. Incluir `transform`+`opacity` preserva o rotate/scale/fade do ícone. Risco nulo: num toggle a página está estática, então nenhum outro transform/opacity muda (transição só dispara em **mudança** de valor); só o ícone (intencional) anima.
- **Sem flash no load / sem afetar reduced-motion:** classe ausente no 1º paint (só entra em ação do usuário) → zero transição no carregamento (não piora nenhum FOUC pré-existente, fora do escopo de P2). Usuários com `prefers-reduced-motion: reduce` ficam fora do `@media no-preference` → swap continua instantâneo (correto p/ a11y).
- **Verificação:** `next build` EXIT=0 (tsc+lint+CSS) · **115/115** vitest · deploy EXIT=0 (SSR function atualizada, demais Skipped — só frontend). **LIVE (prod):** CSS `18cclpdmzeadl.css` contém `@media (prefers-reduced-motion:no-preference){html.theme-transitioning,html.theme-transitioning *,…{transition:background-color .3s,…,transform .3s}}`; o chunk do provider `07yw_j5~98yb3.js` (status 200, mesmo nome local→prod) contém o `setMode` compilado: `useRef(null)`, `classList.add("theme-transitioning")`, `clearTimeout`/`setTimeout` (race-guard). Commit `4e785b9`.

### P1 — registro técnico da correção (2026-06-07)

- **Método:** 2 agentes read-only (Explore) em paralelo auditaram `src/app/learn/*` + `src/components/learn/*` e `src/app/teach/*` + `src/components/teacher/*`, classificando cada tela com fetch por LOADING/EMPTY/ERROR e reportando só GAPS reais ("parece quebrado/blank/crash/spinner infinito") com file:line. **Veredito: cobertura forte no geral** — LearnDashboard, LearnCredentialsHub, LearnerWishlist, LearnCommunityHub, LearnEventsHub, EnrolledCourseWorkspace, SaleList, SaleDetail, CourseBuilderStudio, TeacherCourseStudio, CoursePreviewShell etc. já têm os 3 estados.
- **Toda claim foi re-verificada no código antes de editar** (No-Invention — agentes podem errar linha).
- **Gap 1 (event-studio):** `isLoading` reflete a subscription de **cursos** (`:74`), não a de **eventos** (`:88-92`, sem flag). Logo `events.length === 0` (render `:370`) era true **durante o load** → "No agenda items scheduled yet" piscava p/ professor com sessões. Fix: `eventsLoaded` setado no success **e** error da subscription de eventos; render passa a `!eventsLoaded ? Loading : events.length===0 ? Empty : list`.
- **Gap 2 (wallet):** `isLoading` (só no chip de status Stripe) virava false no profile (`:46`); os totais monetários (hero `teacherNetMinor` `:178`, BalanceRows `:186-188`, MetricCards gross/fee/paid `:278-280`, tabela ledger `:329`) renderizavam **sem gate** a partir de `orders`/`ledgerEntries` que começam `[]` → flash de **$0** numa tela de dinheiro. Fix: `ordersLoaded`+`ledgerLoaded` (setados no success+error de cada subscription), `financialsReady = ordersLoaded && ledgerLoaded`, helper `money(minor)` → `formatMoney` quando ready, senão `—`; tabela ledger ganha estado "Loading payout ledger…" até `ledgerLoaded`. Subscriptions Firestore disparam com snapshot vazio → flags resolvem rápido até p/ conta sem vendas (mostra $0 **correto**, não falso).
- **Deliberadamente NÃO mexido:** loaders inline de texto ("Loading comments…", "Loading community feed…") e o skeleton-pulse das métricas do learner — são estados válidos; o `SkillsetSpinner` canônico tem `min-h-[280px]`, grande demais p/ essas áreas (forçá-lo seria regressão). Não é "quebrado", é estilo diferente e adequado.
- **Verificação:** `next build` EXIT=0 (tsc+lint+CSS) · **115/115** vitest · deploy EXIT=0. Verificação LIVE anônima **não aplicável**: ambas as telas são client-components atrás de auth + flag `teacherStudio.access`, com os componentes pesados em chunks lazy que **não** aparecem no shell público de `/teach` (confirmado: 14 chunks do shell, nenhum com os marcadores). Sem credenciais (política), o teto de verificação é build+test+deploy — todos verdes. Commit `696afff`.

### A5 — registro técnico da correção (2026-06-07)

- **Medição (6 chips × 2 temas, composição alpha real):** font 0.625rem (10px) peso 700 uppercase = **normal text** → exige 4.5:1. LIGHT: warning `#c07b0a` sobre tint 10% âmbar = **3.10:1** ❌, success `#1f8a5b` sobre `--color-success-soft` `#e4f3eb` = **3.78:1** ❌; danger 6.00:1 ✓, info 6.78:1 ✓, draft/inactive 4.89/5.09:1 ✓ (passam **por causa do A3** — o ink-muted já tinha escurecido p/ `#5a6a81`). Confirma o nomeado: só warning+success falham no claro.
- **Por que escurecer o texto (e não clarear o bg):** `#1f8a5b` dá só **4.34:1 até sobre branco puro** — o próprio texto é claro demais. Idem warning (3.45:1 no branco). Clarear o tint não alcança AA; o texto tem que escurecer.
- **Tensão claro↔dark:** `--color-warning`/`--color-success` **não têm override dark** (só os `-soft` têm). No dark os tints viram escuros, então um texto escuro (bom p/ claro) seria quase-preto sobre escuro. Nenhuma cor única passa 4.5:1 sobre fundo claro **e** escuro → **theme-scoped obrigatório**.
- **Fix:** texto base (claro) → warning `#8a5d08` (**5.16:1**), success `#157049` (**5.32:1**); overrides `[data-theme=dark]` → warning `#e6b24f` (**8.05:1**), success `#5fc99a` (**7.85:1**). Net: claro corrigido, **dark também melhora** (warning 4.51→8.05, success 3.69→7.85). `.status-chip__dot{background:currentColor}` herda a cor automaticamente nos 2 temas.
- **Escopo:** A5 fecha os 2 chips nomeados nos 2 temas. Contraste **dark** dos demais chips (danger 2.64:1, draft/inactive ~3.0:1 — tokens semânticos que não trocam) fica no **A1-cont** (`task_469c3fc4`), não no A5 (cujo recorte é o claro warning/success). As cores `#8a5d08`/`#157049`/`#e6b24f`/`#5fc99a` são scoped ao chip; A1-cont pode promovê-las a tokens `-fg`.
- **Verificação:** `next build` EXIT=0. LIVE: bundle minificado tem `.status-chip--warning{…#8a5d08}`, `.status-chip--success{…#157049}`, `[data-theme=dark] .status-chip--warning{color:#e6b24f}`, `…--success{color:#5fc99a}`, e `var(--color-warning)` removido do chip. Commit `e7a42be`.

### A4 — registro técnico da correção (2026-06-07)

- **Bug:** `.signin-dropdown` (`globals.css:672`) e `.account-menu-panel` (`:720`) usavam `background: #ffffff` literal, sem override dark. O conteúdo interno usa tokens que **trocam** no dark (`--color-ink`, `--color-ink-soft`, `--color-primary`, `--color-surface-soft`). Logo, no dark mode: painel branco + texto `--color-ink` (#e8edf5, claro) = **~1.1:1 = invisível**. O trigger (`.account-menu-trigger`, `:731` `rgba(255,255,255,0.72)`) tinha o mesmo defeito — nome ink-claro sobre pílula quase-branca.
- **Fix do painel:** `background` → `var(--color-surface)` (idêntico `#ffffff` no claro = zero mudança; surface escura `#0f1626` no dark). Borda do signin-dropdown `rgba(26,54,93,0.1)` → `var(--color-line)` (consistente com o account-panel, que já usava o token).
- **Cascata tratada (zero-regressão):** escurecer o painel moveu p/ fundo escuro os elementos accent-red que **não trocam** no dark (são a dor do A1-cont): `.account-menu-item--danger` (Sign out, texto+ícone), `.account-menu-context-chip` (`account-menu.tsx:148`), e os `.account-menu-section-label` ink-muted-dark. Medido: vermelho de marca `#b22234` caía p/ **2.73:1** sobre surface escura (era ~6.7:1 sobre o painel branco quebrado). Overrides `[data-theme=dark]` scoped re-colorem: danger/chip/role → `#e06472` (**5.00–5.36:1** compositado sobre os tints reais), section-label → `var(--color-ink-soft)` (**8.48:1**).
- **Trigger:** invertido p/ `rgba(255,255,255,0.06)` rest / `0.10` hover (tint claro sutil sobre header escuro), p/ o nome ink-claro deixar de sentar sobre quase-branco. Nome **14.32:1**, role-red `#e06472` **5.00:1**.
- **Escopo deliberado:** A4 fecha **só** os 2 componentes nomeados (account-menu + signin-dropdown), 100% AA no dark. As **outras ~12 superfícies branco-hardcoded** + o decouple do token semântico de foreground continuam no **A1-cont** (`task_469c3fc4`). `#e06472` é um vermelho-danger dark scoped ao componente; A1-cont pode promovê-lo a `--color-danger-fg`.
- **Verificação:** matriz de contraste compositada (alpha sobre surface/header) — 8 elementos, **todos PASS AA-normal** (5.00–15.36:1). `next build` EXIT=0. LIVE: bundle CSS minificado tem `#e06472`, `.account-menu-panel{…var(--color-surface)…}`, `.signin-dropdown{…var(--color-surface)…}`, `[data-theme=dark] .account-menu-trigger{background:#ffffff0f}` (minifier reescreveu `rgba(255,255,255,.06)`→`#ffffff0f`). Commit `c41666f`.

### A3 — registro técnico da correção (2026-06-07)

- **Bug medido (não estimado):** `--color-ink-muted: #7a8fae` (light, `globals.css:17`) reprovou WCAG AA-normal (4.5:1) em **todos** os fundos claros — script de luminância sRGB: branco **3.30:1**, surface-soft `#eef4fc` **2.98:1**, surface-strong `#e3eef9` **2.81:1**, chips draft/inactive **2.94/3.05:1**. O backlog citava só "fundo claro"; a medição mostrou que falhava até no branco.
- **Fix:** escurecimento **uniforme 0.74×** do valor original → `#5a6a81`. Multiplicação uniforme dos 3 canais preserva o hue exato (ratios 0x5a/0x7a=0.738, 0x6a/0x8f=0.741, 0x81/0xae=0.741) — mesma identidade de cor, só mais escura. Resultado AA-normal: branco **5.51:1**, soft **4.98:1**, strong **4.68:1**, chips **4.90/5.09:1** — passa em tudo. L=0.1407 vs `--color-ink-soft` L=0.1297: muted continua um degrau mais claro, hierarquia ink>soft>muted preservada.
- **Auditoria de regressão (16 usos):** 7 CSS (`:367,391,799,1230,1947,3328,3812`) + 9 JSX (`text-[var(--color-ink-muted)]` em account/admin/teacher/auth/platform/pricing). **Nenhum** usa muted como texto sobre fundo escuro/colorido — todos são label/placeholder/sub-texto/ícone sobre branco ou surface-soft. Os 2 chips (`--draft`/`--inactive`) têm `background: rgba(122,143,174, .12/.08)` = tint da própria cor **sobre branco** (≈near-white); como o rgba é hardcoded e não referencia o token, escurecer o texto só **aumenta** o contraste. Os 2 `bg-[var(--color-ink-muted)]` são dots sobre branco (ficam mais visíveis). Único decorativo grande (`how-it-works-strip.tsx:61`, número 6xl) já passava como large-text e continua. **Zero regressão.**
- **Escopo dark:** `--color-ink-muted` **tem override próprio no dark** (`#6b7e9a`, `globals.css:54`) — diferente dos tokens semânticos de A1-cont que não trocam. Logo o fix do valor claro **não toca** o dark theme (cujo contraste é auditoria à parte). LIVE confirmou `#6b7e9a` intacto.
- **Verificação:** `next build` EXIT=0 · script LIVE varreu o bundle CSS (`/_next/static/chunks/*.css`) em `/promise` e `/pricing`: `--color-ink-muted:#5a6a81` presente, `#7a8fae` ausente, `#6b7e9a` intacto. Commit `6fe39b6`.

### A2 — registro técnico da correção (2026-06-07)

- **Bug:** as três telas de credenciais renderizavam a mensagem de erro como `<p className="...text-accent">{error}</p>` sem `role`/`aria-live` — `login-form.tsx:108`, `signup-form.tsx:245`, `reset-password-form.tsx:44`. Sem live region, o leitor de tela não reanuncia quando o `<p>` é inserido após submit falho. O `reset-password-form` também tinha um `<p>` de **sucesso** (`:49`) igualmente mudo.
- **Fix (ARIA/WCAG best practice):** erro → `role="alert"` + `aria-live="assertive"` (alert já implica assertive+atomic; o `aria-live` explícito reforça em AT antigo). Sucesso → `role="status"` + `aria-live="polite"`. Severidade do canal casa com a semântica: falha de auth **interrompe**, confirmação **não**.
- **Por que anúncio-na-inserção é suficiente aqui:** o erro surge em resposta a um submit num form **estável** (sem troca de rota/remontagem de página) — NVDA/JAWS/VoiceOver anunciam nós `role=alert` recém-inseridos nesse cenário. Os handlers fazem `setError("")` no início → transição vazio↔texto garante mudança de conteúdo detectável.
- **Escopo:** ampliado do único arquivo nomeado (`login-form`) para a superfície inteira de credenciais (login + signup + reset) — mesma classe de bug, fix byte-idêntico. Um único `error` state por form cobre todas as mensagens (signup tem 6 `setError` client-side → 1 render).
- **Zero regressão visual:** apenas atributos ARIA adicionados; nenhuma classe/markup visível mudou. `next build` EXIT=0 · **115/115** vitest · eslint limpo.
- **Verificação LIVE:** script fetchou `/login` e `/forgot-password`, varreu os 11 chunks de cada, localizou o chunk do form pelo marcador (`you@example.com` / `Send reset link`) e confirmou `role="alert"`+`aria-live=assertive` em ambos, e `role="status"`+`polite` no de reset. Commit `0979b29`.

### T2 — registro técnico (2026-06-06)

- **Premissa do backlog ("stubs no nav / esconder do menu") era stale.** Prova: `platform-nav.tsx:64-68` filtra por `item.contexts.includes(context)` e `context ∈ {learner,teacher,ops}` (nunca vazio). Os 5 itens com `contexts: []` em `site.ts` (`coupons:141`, `co-productions:149`, `refunds:157`, `team:165`, `integrations:173`) → `[].includes(...)` = `false` → **nunca renderizados no nav**. "Esconder do menu" já era verdade.
- **Stubs já honestos:** os 4 (`integrations`/`co-productions`/`team`/`coupons`) renderizam `TeacherComingSoonPanel` sob `ProtectedSurface` (gate `teacherStudio.access`), com texto "on the roadmap" + CTA real ("Manage payouts" → `/account/payments`). Alcançáveis só por URL direta; **zero `<Link>`** aponta para eles. Mudá-los seria **inventar trabalho** → mantidos como estão.
- **Defeito real (corrigido):** `legal/terms/page.tsx:103-106` afirmava *"Educator payout and refund handling is described in the **educator refund policy**"* linkando `/teach/refunds` — que **não é página de política**, é `redirect("/account/payments")` (auth-gated). Um visitante deslogado lendo os Termos clicava e batia num login, não numa política. **Fix:** repontado para `/fees-and-payouts` (página **pública**, descreve refund window 7d + payout clearance 30d + fees + disputes) e relabel para "fees and payouts policy". Agora `/teach/refunds` tem **zero links inbound** (só a entrada hidden em `site.ts:154`).
- **Verificação:** grep confirma 0 links para os 4 stubs e 0 links para `/teach/refunds` fora da entrada hidden · `eslint src/app/legal/terms/page.tsx` ✅.
- **Aberto (opcional, fundador) — RESOLVIDO:** (a) ✅ **feito** (ordem 4): "Notify me" via `mailto` é um sinal de demanda honesto, não uma promessa de feature — escolhido como o opcional mais recomendado; (b) deletar a rota órfã `/teach/refunds` — **descartado**: o redirect gracioso evita 404 para URL digitada/indexada, melhor que remover (decisão B do demo-readiness mantida).

### B1 — registro técnico da correção (2026-06-06)

- **Bug:** `markStripeEventProcessed` gravava o marker ANTES do handler. Falha transitória → 500 → Stripe re-tenta o mesmo `event.id` → marker já existe → short-circuit como "duplicate" → handler nunca re-roda → **enrollment/payout perdidos para sempre**.
- **Pré-requisito verificado:** os **5 handlers** que um reprocesso pode tocar são idempotentes — `handleCheckoutCompleted` (writes merge/update; enrollment guardado por `if (!exists)`), `handleChargeRefunded` (reversal com `idempotencyKey` estável `..._${charge.id}_${charge.amount_refunded}` + `alreadyReversed` lido do ledger commitado → nunca dobra), `markOrderStatus`/`syncSubscriptionFromStripe`/`handleInvoicePaymentFailed` (puro `set(merge)`). Reprocessar é seguro; único artefato é linha de auditoria/analytics duplicada (cosmético). **Nenhum dinheiro se move neste handler** — o transfer real é o cron `dailyReleaseTransfers` em D+30, muito depois da janela de retry da Stripe (~3 dias).
- **Fix:** two-phase. Claim grava `status:"processing"`; promoção a `status:"done"` **só após** todos os handlers rodarem sem throw; short-circuit **apenas** em `done`. Marker preso em `processing` (throw ou crash entre claim e completion) é reprocessado no retry.
- **Arquivos:** `payment-rules.ts` (+`claimStripeEvent`, `markStripeEventDone`, `decideStripeEventClaim`, tipos `StripeEventMarkerRef`/`StripeEventMarkerStatus`/`StripeEventClaimDecision`); `index.ts` (import + handler claim/complete + remoção da `markStripeEventProcessed`); `payment-rules.test.ts` (+5 testes, incl. o guard exato da regressão "retry após falha = reprocessa, não duplicate").
- **Verificação:** `npm --prefix functions run build` (tsc) ✅ · `vitest run` 97/97 ✅ · `eslint` (3 arquivos) ✅.

### B2 + B3 — registro técnico da correção (2026-06-06)

- **B2 (overwrite fora de ordem).** Stripe não garante ordem de entrega: um `checkout.session.expired`/`payment_failed` de uma tentativa antiga pode chegar **depois** do `checkout.session.completed` já ter marcado `paid`, revogando uma compra real. **Fix:** `markOrderStatus` agora roda em `db.runTransaction` e congela estados terminais (`paid`/`refunded`/`partially_refunded`) via `shouldApplyOrderStatusTransition` (puro, testado). O read do pedido precede todo write na transação.
- **B3 (compra duplicada não-transacional).** `enrollmentRef.get()` é leitura fora de transação; dois checkouts concorrentes (duplo-clique/duas abas) passavam ambos → cobrança dupla. **Fix:** lock `checkoutLocks/${userId}__${courseId}` reivindicado **dentro de uma transação** — a concorrência otimista do Firestore serializa: exatamente um vencedor segue, perdedores re-leem o claim fresco e resolvem para `reuse` (devolve a MESMA url de sessão) ou `wait`. Decisão pura em `decideCheckoutLock` (testada); a serialização é propriedade da transação.
- **Race pego pelo review adversarial (antes do LIVE).** A primeira versão re-reivindicava o lock no path de *takeover* com `.set({merge:true})` — que **não** falha quando o doc existe, logo dois deciders de takeover sobre um lock stale ambos seguiam para `stripe.checkout.sessions.create` → **double charge**. Movido para dentro da transação (o doc do lock entra no read-set → o perdedor aborta, re-tenta e cai em `wait`/`reuse`). Re-verificado por um segundo agente adversarial: **SAFE-TO-DEPLOY**, sem race residual.
- **Hardening do mesmo review:** (1) a sessão Stripe agora seta `expires_at` (~31 min) para que o `sessionTtlMs` do lock (35 min) seja **honesto** — um takeover nunca corre contra uma sessão ainda pagável (antes, sem `expires_at`, a sessão real vivia ~24h); na expiração o `checkout.session.expired` cancela o pedido e libera o lock. (2) release de lock (em `markOrderStatus` **e** `handleCheckoutCompleted`) é guardado por `shouldReleaseCheckoutLock(lock.orderId === orderId)` — um evento terminal tardio de uma tentativa **antiga** nunca derruba o lock de uma **re-compra viva**. (3) o cron `expireStalePendingOrders` roteia por `markOrderStatus`, herdando o freeze e o release com guarda de posse (sem locks órfãos).
- **Arquivos:** `payment-rules.ts` (+`shouldReleaseCheckoutLock`); `index.ts` (`createCheckoutSession` lock transacional + `expires_at`; `markOrderStatus` transacional + guarda de posse; `handleCheckoutCompleted` release guardado; cron via `markOrderStatus`); `payment-rules.test.ts` (+3 testes de posse, incl. o path exato "evento de A não derruba lock de B").
- **Verificação:** `npm --prefix functions run build` (tsc) ✅ · `vitest run` **106/106** ✅ · `eslint .` ✅ · review adversarial 3-lentes (`fix-first`) → fixes aplicados → re-review (`SAFE-TO-DEPLOY`).

---

## 🔴 Backend — hardening do fluxo de dinheiro v2 (2026-06-07 → 06-08)

> **Contexto.** Depois de B1/B2/B3, um **incidente LIVE** (onboarding Stripe do professor retornando **500 opaco**) expôs um footgun de credencial e disparou uma auditoria adversarial focada no rail de **payout/refund de assinatura** (Stripe Connect, API **Basil `2026-02-25.clover`**, SDK `stripe ^20.4.1`). Resultado: 1 bug de credencial (SEC1) + 4 gaps de fluxo de dinheiro (G1–G4). Cada fix foi verificado por **review adversarial multi-lente** com evidência `file:linha` de leitura direta (No-Invention) — incluindo **defeitos que o próprio review pegou DENTRO do fix** (G2 sofreu 3 rounds).
> **Ground truth desta sessão:** `functions tsc` exit 0 · `vitest run` **164/164** (de 92) · `payment-rules.test.ts` **41/41** (de 25) · `test:rules` (emulador) **60/60** · `eslint` limpo · deploy **`npm run deploy:app`** → ~40 functions + `stripeWebhook` *Successful update*, hosting *release complete*.
> **Descoberta-chave do Basil (verificada nos type defs instalados `functions/node_modules/stripe/types/`):** o campo top-level `invoice.payment_intent` **foi REMOVIDO**; o PI agora vive em `invoice.payments.data[].payment.payment_intent` (`Invoices.d.ts:343` — `payments?:` é **opcional** e um sub-recurso expansível **não serializado** no payload do webhook). Nem `Charge.invoice` nem `PaymentIntent.invoice` existem no Basil → num evento `charge.refunded` o **PaymentIntent é a única chave de join** até o ledger. Isso é a raiz de G2.

| # | Item | Arquivo:linha ✓ | Sev | Status |
|---|------|-----------------|-----|--------|
| SEC1 | **Secret com `\n`/espaço → 500 opaco** — chave Stripe com newline final injeta caractere inválido no header `Authorization` (`ERR_INVALID_CHAR`), que chega ao cliente como 500 INTERNAL sem causa. Quebrou o onboarding LIVE do professor (2026-06-07). | `payment-rules.ts:89` + `index.ts:651` | **alta** | ✅ corrigido |
| G1 | **Refund parcial ANTES do release estranha o payout** — um refund parcial pré-release não reduzia o transfer planejado nem reabria o status, podendo liberar net cheio sobre cobrança já parcialmente reembolsada. | `payment-rules.ts:112,182,278` + `index.ts:178-216` | **alta** | ✅ corrigido |
| G2 | **Refund de assinatura via dashboard NUNCA faz clawback** — ledger de assinatura gravava `paymentId = invoice.payment_intent` (`undefined` no Basil) → query do clawback (`paymentId == charge.payment_intent`) nunca casava → plataforma reembolsa o aluno mas **nunca recupera o payout do professor**. | `payment-rules.ts:232` + `index.ts:4094-4188,4405` | **crítica** | ✅ corrigido |
| G3 | **`async_payment_*` ausente + re-arm de ledger terminal** — métodos de pagamento atrasado (débito/voucher) fulfilam só no `async_payment_succeeded` (faltava handler → aluno paga e não recebe o curso); e uma 2ª conclusão do mesmo pedido reabria o ledger terminal com `releaseAt` fresco. | `index.ts:3628,3646,3834` | **alta** | ✅ corrigido |
| G4 | **Gate de acesso de não-pagante (regressão)** — testes de regra travando que `status ∈ {refunded, …}` não acessa conteúdo protegido (sem mudança de regra; trava de comportamento). | `tests/firestore-rules.ts` | **média** | ✅ testes |

### SEC1 — registro técnico (secret-newline footgun, 2026-06-07)

- **Bug (incidente LIVE).** A secret do Stripe configurada com um `\n` final (paste de painel/`.env` com quebra) era passada crua ao construtor do SDK → o header `Authorization: Bearer sk_live_…\n` dispara `TypeError [ERR_INVALID_CHAR]` no Node, que **só chega ao caller como um 500 INTERNAL opaco** (`payment-rules.ts:78-80`). Sintoma: o professor clicava "conectar Stripe" e recebia erro genérico, sem causa rastreável.
- **Fix (puro, testado).** `sanitizeStripeSecret(raw)` (`payment-rules.ts:89`) faz `trim()` e valida que a chave é **ASCII imprimível sem espaço interior** (`/[^\x21-\x7e]/`): retorna `{ok,key}` ou um motivo (`"missing"` | `"malformed"`) que o caller mapeia para erro **legível** em vez de 500. Cableado no boot do cliente Stripe (`index.ts:651`). Um `\n` final é corrigido pelo trim; corrupção interior (espaço no meio) é rejeitada como `malformed` com instrução de re-setar a chave numa linha limpa.
- **Verificação:** vitest cobrindo missing / trailing-newline / interior-space / chave válida · `tsc` ✅ · `eslint` ✅.

### G1 — registro técnico (refund parcial antes do release, 2026-06-07)

- **Bug.** O rail de release (`dailyReleaseTransfers` → `claimLedgerForRelease` → `releaseLedgerTransfer`) liberava `netAmountMinor` cheio. Se um **refund parcial** chegasse **antes** do release (dentro da janela de 30d), o transfer ainda saía pelo net integral, e o status do ledger não refletia o reembolso parcial — descasamento entre o que foi reembolsado ao aluno e o que foi pago ao professor.
- **Fix (matemática pura, set-once).** (1) `plannedReleaseTransferAmountMinor({net,gross,refunded})` (`payment-rules.ts:182`) = `net` se `refunded≤0`, senão `floor(net*(gross−refunded)/gross)`. (2) `releasedRefundReversalAmountMinor(...)` (`payment-rules.ts:112`) ganhou ramo **net-based** (só quando `net` dado E `transferred < net`): reverte com base no que realmente saiu, não no net cheio. (3) `ledgerRefundStatus(isFull,current)` (`payment-rules.ts:278`) — **anti-stranding**: um refund parcial sobre um ledger ainda `in_release` **preserva** `in_release` (deixa o cron liberar o saldo reduzido) em vez de prendê-lo em `partially_refunded`. (4) O valor de transfer é **computado uma vez** no claim e gravado (`plannedTransferAmountMinor`/`transferAmountMinor`, `index.ts:178-216`) → retries do mesmo release movem valor idêntico sob idempotency key estável.
- **Design-around (race) pego na leitura adversarial ANTES do edit:** a query do cron (`status == "in_release"`, sem filtro de kind) ficou **inalterada** — refunds parciais pré-release continuam `in_release` por um discriminador de status fresco, evitando duplo-transfer.
- **Verificação:** +6 testes em `payment-rules.test.ts` (reversal net-based, plannedTransfer, ledgerRefundStatus) · `tsc` ✅ · `vitest` ✅.

### G2 — registro técnico (clawback de refund de assinatura + 3 rounds adversariais, 2026-06-07 → 06-08)

- **Bug (crítico).** Ledger de assinatura (`handleCourseSubscriptionInvoicePaid`) gravava `paymentId` a partir de `invoice.payment_intent` — campo **removido no Basil** → `undefined` → fallback para `invoice.id`. O clawback (`handleSubscriptionChargeRefunded`, `index.ts:4405`) busca `payoutLedger where paymentId == charge.payment_intent`; com a chave degradada (`in_…`) a query **nunca casa** → num refund de assinatura pelo dashboard, a plataforma estorna o aluno mas **nunca recupera o transfer do professor** (vazamento de dinheiro permanente).
- **Round 1 (review adversarial → CONFIRMADO crítico, 2 lentes mesma raiz).** Fix: `resolveInvoicePaymentIntentId(invoice)` (`payment-rules.ts:232`) percorre `invoice.payments?.data[].payment.payment_intent` (Basil) com fallback ao legacy top-level; em `handleCourseSubscriptionInvoicePaid` resolve inline → **expand-retrieve** → fallback. Novo handler `handleSubscriptionChargeRefunded` (`index.ts:4405`) faz o clawback do transfer liberado (`shouldReverseReleasedPayout` + `createReleasedRefundTransferReversal`, idempotency key `transfer_reversal_${ledger}_${charge}_${amount_refunded}`), grava SÓ o ledger (re-read transacional + `ledgerRefundStatus`), **sem** revogar matrícula. (1 finding refutado: ordem charge-vs-invoice era pré-existente e inalterada.)
- **Round 2 (review do PRÓPRIO fix → CONFIRMADO high, defeito DENTRO do fix).** Como `invoice.payments` **não** vem inline no webhook, o inline resolve retorna `null` em TODO invoice.paid normal → o `invoices.retrieve(...,{expand:["payments"]})` é o caminho **primário de fato**. Estava num `try/catch` que **engolia** o erro → caía em `paymentId = invoice.id` (degradado) → handler retorna 200 → marker de idempotência promovido a `done` → Stripe **nunca redelivra** → vazamento permanente reintroduzido por falha transitória. Fix: na falha do retrieve, **lançar** quando um ledger com chave de join será escrito (`grossAmountMinor > 0 && connectedAccountId`, `index.ts:4112`) → webhook 500 → marker fica `processing` → Stripe redelivra → self-heal. Espelha o irmão seguro `stripe.subscriptions.retrieve` (não-wrapped) no mesmo handler. Sem ledger a escrever ($0/sem conta) → loga e segue (não bloqueia enrollment).
- **Round 3 (review do fix corrigido → GO, 2 de 3 lentes `sound`).** A 3ª lente apontou um HIGH residual: retrieve **sucede** mas sem PI + ledger a escrever → grava chave degradada. **Disposição:** sob Basil, "retrieve ok + sem PI" ⇒ fatura sem charge reembolsável ⇒ nenhum `charge.refunded` com `payment_intent` jamais dispara ⇒ a linha degradada (`in_…`) é **inerte por namespace** contra a query do clawback (que só casa `pi_…`). **Não** é vazamento ativo. **Não** apliquei as sugestões literais do verificador (`&& resolvedPaymentIntentId` no gate, ou throw) porque **ambas regridem** o caso legítimo de credit-balance (gate **subpaga** o professor; throw **poison-pilla** o redelivery E bloqueia o enrollment do aluno pagante). **Mitigação aplicada (upside puro, zero regressão):** flag auditável `paymentIdIsPaymentIntent: Boolean(resolvedPaymentIntentId)` no ledger (`index.ts:178-216` tipo + write `~:4188`) → o invariante "sem PI ⇒ sem charge reembolsável" deixa de viver só num comentário e fica **consultável em dados** (`payoutLedger where paymentIdIsPaymentIntent == false`).
- **Cadeia de idempotência verificada em código real, ponta a ponta:** throw → catch do webhook (`index.ts:3702`) → `response.status(500)` → `markStripeEventDone` (`:3698`) pulado → marker `processing`; no redelivery `claimStripeEvent` → `decideStripeEventClaim` (`payment-rules.ts:436`) retorna `process` (não `duplicate`) para `processing` → reprocessa.
- **Verificação:** +5 testes `resolveInvoicePaymentIntentId` + testes de `shouldReverseReleasedPayout`/`ledgerRefundStatus` · `tsc` ✅ · `vitest` **164/164** · `eslint` ✅ · 3 workflows adversariais (round1 fix-first, round2 fix-verify, round3 3-lentes → **GO**).

### G3 — registro técnico (`async_payment_*` + re-arm guard, 2026-06-07)

- **Bug A (fulfilment perdido).** Métodos de pagamento atrasado (débito bancário, voucher) completam a Checkout Session como `unpaid` primeiro — `handleCheckoutCompleted` os difere — e só fulfilam quando os fundos limpam via `checkout.session.async_payment_succeeded`. Sem esse handler, o aluno paga e **nunca recebe o curso**. Fix: handlers `async_payment_succeeded` (`index.ts:3628`, modo subscription → log; senão `handleCheckoutCompleted`) e `async_payment_failed` (`:3646` → `markOrderStatus(...,"failed")`, libera o lock de checkout).
- **Bug B (re-arm de ledger terminal).** Uma 2ª conclusão do mesmo pedido (redelivery, ou `async_payment_succeeded` após refund) re-rodava o fulfilment → flipava o pedido de volta a `paid`, resetava o ledger a `in_release` com `releaseAt` **fresco** → re-agendava payout de dinheiro talvez já reembolsado. Fix: **re-arm guard** (`index.ts:3834`) — como o ledger é criado exatamente uma vez por pedido nesta transação atômica, `if (ledgerSnapshot.exists) return` (idempotente).
- **Verificação:** `tsc` ✅ · `vitest` ✅ · `eslint` ✅.

### G4 — registro técnico (gate de acesso de não-pagante, 2026-06-07)

- **Objetivo.** Travar por **teste de regra** (não por mudança de regra) que um usuário com enrollment não-pagante (`status` refunded/cancelled/pending) **não** lê conteúdo protegido nem cria comentário de aula — fechando a porta a uma regressão silenciosa futura nas Firestore rules.
- **Fix:** testes de regressão em `tests/firestore-rules.ts` (gate por status de enrollment) — denials assertados rodam no emulador. Sem alteração em `firestore.rules`/`storage.rules` (logo `deploy:rules` **não** necessário; `deploy:app` é suficiente).
- **Verificação:** `test:rules` (emulador) **60/60** ✅.

### C2 — registro técnico da correção (2026-06-07)

- **Objetivo.** Skool ostenta "Google-indexed" como vantagem. Nós tínhamos **0 JSON-LD** (`grep schema.org/@type`=0) e as páginas `/courses/[slug]` **fora do sitemap**. C2 fecha os dois: dados estruturados `schema.org/Course` para rich results + as 6 rotas de curso no sitemap, tudo build-time/SSR (só catálogo estático; cursos de criador resolvem client-side, fora de escopo sem Admin SDK).
- **Furo de No-Invention pego pelo review adversarial (antes do LIVE).** A 1ª versão emitia `Offer` com `availability:"https://schema.org/InStock"` sempre que `priceAmountMinor` fosse numérico — **independente** de o checkout estar ligado. Se o flag `payments.checkout` estivesse off, o card mostraria "Checkout opening soon" enquanto o JSON-LD diria InStock → mismatch structured-data ↔ conteúdo (penalidade Google + overclaim Art. IV). **Fix:** `buildCourseJsonLd(course, { purchasable, siteUrl? })`; o `Offer` só é emitido quando `purchasable` — derivado do **mesmo `getCourseAccessDecision(course, isPublicFeatureEnabled("payments.checkout"))`** que o `CourseEnrollmentCta` renderiza. Fonte única de verdade → paridade por construção, nas duas direções.
- **Achado de produção (evidência > suposição).** A verificação LIVE provou que `payments.checkout` está **ligado** no build de produção: os 3 cursos pagos auto-matriculáveis (leadership 149.00, mental-health 49.00, communication 79.00) emitem `Offer InStock` — **verdadeiro**, checkout está vendendo de verdade (valida retroativamente a urgência do fix B2/B3). Os 3 não-abertos/sem-preço (clinical-psychology, hr-management, project-management) emitem **zero `Offer`** — corretamente gateados.
- **Sem invenção de campo:** sem `aggregateRating`/`review` (catálogo não tem nota) e sem `hasCourseInstance` (`durationLabel` é texto livre "8-12 weeks", não ISO-8601). `inLanguage:"en"` documentado como default site-wide (não há campo de idioma por curso). Imagem relativa absolutizada; `<` escapado no `<script>` (XSS-safe).
- **Arquivos:** `src/lib/seo/course-jsonld.ts` (novo, builder gateado); `src/components/seo/json-ld.tsx` (novo, componente `<script ld+json>`); `src/app/courses/[slug]/page.tsx` (computa `purchasable` server-side + renderiza `JsonLd`); `src/app/sitemap.ts` (6 rotas de curso via `getCourseSlugs`); `src/app/robots.ts` + `src/app/sitemap.ts` (importam `SITE_URL` de `page-metadata`, dedup); `src/lib/seo/page-metadata.ts` (`export const SITE_URL`); `src/lib/seo/course-jsonld.test.tsx` (novo, 9 testes incl. regressão purchasable=false).
- **Verificação:** tsc ✅ · `vitest run` **115/115** ✅ · `eslint` (5 arquivos) ✅ · deploy `npm run deploy:app` (SSR `ssrskillsetusaofficial` atualizado, hosting released) ✅ · **LIVE**: `sitemap.xml`=200 com 6 rotas de curso, `robots.txt`=200 com Sitemap+Host, 6 páginas de curso=200 cada com exatamente 1 bloco `Course`, Offers presentes só nos 3 purchasable (parity sem violação).

### T3 — registro técnico da correção (2026-06-07)

- **Premissa parcialmente stale.** O backlog (`promise/page.tsx:17`) apontava 5 features não-shipadas no "feature parity": affiliate, analytics, quizzes, drip, custom domains. O commit `257ff4c` (anterior a esta sessão) **já tinha removido custom-domains, analytics e affiliate** — mas **manteve quizzes, drip e community/certificates** afirmando "all verified live". Re-verifiquei essa afirmação (No-Invention: não confiar, conferir).
- **Verificação feature-por-feature:** **certificates** — shipped (funcs `issueSkillsetCertificate`/`verifySkillsetCertificate` + `domain/certificate.test.tsx`). **drip content** — shipped end-to-end (`domain/drip-policy.ts`, 5 estratégias, `getLessonUnlockState`; wired no teacher `course-builder-studio.tsx` **e** no learner `enrolled-course-workspace.tsx`). **community** — shipped (surface extensa `/learn/community/*`, feed, moderação). **quizzes** — **NÃO funcionalmente shipped**: `quiz` é `LessonType` selecionável no builder, mas o learner renderiza placeholder (`enrolled-course-workspace.tsx:722`: "...when assessment tools are **connected**"). Nota: video/text/etc. dizem "when the instructor attaches/publishes" (feature pronta, aguarda conteúdo) — quiz é o único que admite que a **tooling** não existe.
- **Fix:** removido só "quizzes" do promise #02, consistente com como `257ff4c` tratou os outros não-shipados. Texto final: "...locking certificates, drip content, or community."
- **Reachability do dead-end de quiz (avaliada, latente):** `getPublicFeatureFlagOverrides()` (`feature-flags/index.ts:187-200`) só faz override de `payments.checkout` via `NEXT_PUBLIC_PAYMENTS_CHECKOUT_ENABLED`. **Não há override** para `teacherStudio.courseBuilder` (`defaultEnabled:false`) → sempre off em prod → **nenhum criador alcança o builder** → nenhum aluno bate no placeholder de quiz hoje. `/promise` era a **única** superfície pública live anunciando quizzes (grep confirmou: nenhuma outra página de marketing menciona quiz).
- **⚠️ Gap conhecido para o futuro (founder):** quando `teacherStudio.courseBuilder` for ligado, a opção "quiz" do builder (`lesson-content-modal.tsx:67`, `course-builder-studio.tsx:252`) precisa ser **terminada (assessment tooling)** ou **escondida** — senão um criador autora um quiz que vira placeholder para o aluno. Não é defeito live hoje; é pré-condição do lançamento do teacher studio.
- **Verificação:** tsc ✅ · `eslint src/app/promise/page.tsx` ✅ · 5/5 testes de página (`marketing` + `platform-pages`) ✅ · deploy `npm run deploy:app` ✅ · **LIVE**: `/promise` sem nenhum substring "quiz".

---

## 🟪 Community finish (C7 + C8) — registros de BUILD (2026-06-08)

### C7 — registro técnico do BUILD (2026-06-08) — diretório de membros (roster real, privacy-safe)

- **Premissa verificada (No-Invention).** A aba "members" do feed era um **stub estático** `CommunityInfoPanel` (`course-community-feed.tsx:291-300` pré-edição) — 3 bullets de texto ("A richer member directory comes after…"). Zero dado real. Substituído por um roster **derivado de dados que o pai já assina** (zero fetch novo, zero regra nova, zero exposição de privacidade nova).
- **Decisão de escopo honesta (a restrição que define o desenho).** Não dá para listar a **matrícula** de um curso no cliente: leituras de `enrollments` são *own-only* por design (privacidade). E `memberStats` é uma coleção **global** (não tem recorte por curso). Logo o roster fiel ao que existe = **contribuidores do espaço** (autores distintos dos posts carregados) **× standing global** (`memberStats`). Rotulado com honestidade: "People active in this space, ranked by their Skillset level" — **não** finge ser a matrícula inteira, **não** inventa status "online".
- **Implementação (`CommunityMembersPanel`, aditivo).** Dedup de `postsState.posts` por `authorId` (Map, primeiro-post-vence p/ nome/role) → join com o `Map<string, MemberStats>` já assinado no pai (`subscribeToMemberStatsMap`, `course-community-feed.tsx:100`) → ordena por `points` desc, depois nome → render com `LevelBadge`, pontos, likes recebidos, highlight "You", rank, e estado-vazio honesto. `useMemo` sobre `[posts, memberStats]`. Fonte de dados idêntica à do leaderboard (que **já** expõe `memberStats` global a qualquer signed-in) → **sem nova superfície de exposição**.
- **Dark-safe por construção:** o painel usa `bg-[var(--color-surface)]` (= `#fff` no claro, `#0f1626` no dark — `globals.css:5/56`) em vez de `bg-white` hardcoded → não adiciona dívida de dark mode.
- **Verificação:** tsc FE (`tsc --noEmit -p tsconfig.json`) EXIT 0 · `eslint course-community-feed.tsx` EXIT 0 · **164/164** `vitest run` ✅. (Roster é auth+matrícula-gated → E2E LIVE N/A sem credenciais; teto = build verde + paridade de fonte com o leaderboard já-live.)

### C8 — registro técnico do BUILD (2026-06-08) — posts fixados (teacher-curated) + respostas aninhadas

- **Premissa verificada (No-Invention).** Posts (`communityPosts/{id}`) não tinham `pinned`; comentários (`…/comments/{id}`) eram **planos** (sem `parentId`). Confirmado em `domain/community-post.ts` e nas regras (`firestore.rules` `hasOnlyAllowed…Keys`). `courseId === courseSlug` provado: `firestore.rules:1508/1543` passam `courseId` para `hasEnrollmentForCourseSlug` (que monta a chave a partir de um slug) → `courses/{courseSlug}` é doc válido e `courseData(courseSlug).ownerId` identifica o professor **sem inventar relação de schema**.
- **Regras — caminho teacher-pin (escrito apertado, security-critical).** Novo `teacherCanPinCommunityPost()`: `ownsCourse(resource.data.courseSlug)` (lê o slug **armazenado** → professor nunca fixa post de curso que não é dele) `&&` `request.resource.data.get('pinned', false) is bool` `&&` `diff().affectedKeys().hasOnly(['pinned','updatedAt'])` (clamp → professor **não** edita corpo/autor/categoria enquanto fixa). Ordenado **antes** do caminho-autor no `allow update` (curto-circuito; `validCommunityPostShape` do caminho-autor exigiria `authorId==auth.uid`, que barraria o professor).
- **Regras — invariantes anti-abuso.** (1) **Create** ganha `request.resource.data.get('pinned', false) == false` → ninguém cria post pré-fixado. (2) **Caminho-autor** ganha `request.resource.data.get('pinned', false) == resource.data.get('pinned', false)` → autor edita o próprio corpo mas **não** auto-fixa nem desfixa (só professor-dono/admin mexem em `pinned`). (3) `validCommunityPostShape` aceita `pinned` **opcional, bool quando presente**. (4) Comentário: `parentId` opcional, `null | string` — 1 nível de aninhamento é display-concern (UI), regra só faz type-check.
- **Data layer.** `setCommunityPostPinned(postId, pinned)` escreve **só** `{pinned, updatedAt}` (casa o clamp `hasOnly`). `compareFeedPosts` ordena **pinned-first, depois createdAt desc** no cliente (mantém a query num único filtro `courseSlug`, sem índice composto, e correto p/ posts legados sem o campo). `createCommunityComment` aceita `parentId?: string|null` (default `null`, sempre enviado p/ satisfazer a shape).
- **UI (`course-community-feed.tsx`).** **Render fixado é universal** (todo viewer vê o badge "Pinned" + float-to-top — o valor 80%). O **controle** pin/unpin é gated por uma nova prop `canModerate` (default `false`); só o **mount do criador** (`creator-course-community.tsx`) a passa como `course.ownerId === user.uid` — a regra `ownsCourse` é a autoridade real, a prop só evita afetar afford. **Respostas aninhadas:** `CommentNode` (estado de form de reply próprio por thread) + `CommentBody` (linha de autor/badge/corpo/report reutilizada por raiz e reply); `useMemo` threada a lista plana em 1 nível resolvendo cada reply ao **ancestral-raiz** (sobe a cadeia `parentId` com **cycle-guard de 16**) → reply órfã/profunda renderiza sob uma raiz real em vez de sumir.
- **Cobertura de testes (10 novos, emulador).** owner-pode-fixar (professor **não** matriculado, só dono) · não-dono-matriculado-barrado · autor-auto-fixar-barrado · autor-edita-corpo-ainda-ok · clamp-`hasOnly`-barra-edição-de-corpo-ao-fixar · create-pré-fixado-barrado · comentário-raiz(`parentId null`)-ok · reply-aninhado(`parentId string`)-ok · `parentId`-tipo-errado-barrado.
- **Verificação:** tsc FE EXIT 0 · `eslint` (4 arquivos) EXIT 0 · **69/69** `test:rules` (firestore 57 + storage 12; os 10 C8 verdes, `PERMISSION_DENIED` no stderr = denials esperados dos `assertFails`) · **164/164** `vitest run`.
- **⚠️ DEFERIDO → decisão do fundador (ponto de entrada de moderação do professor).** O render fixado serve todos hoje; mas o **botão** pin/unpin só aparece onde o `ownerId` é conhecido no cliente — hoje o `creator-course-community.tsx`, que **gateia por matrícula** (`subscribeToEnrollment(user.uid, courseId)`). Se professores **não** são auto-matriculados no próprio curso, esse mount pode não ser o lugar onde o professor sempre alcança o botão. **Decisão necessária:** (a) auto-matricular o professor-dono no espaço do próprio curso (mais simples; o botão "só funciona"), **ou** (b) uma superfície dedicada de moderação do professor que carregue `ownerId` sem exigir matrícula. A regra (`ownsCourse`) já está correta e testada nos dois casos; é puramente *onde a UI expõe o controle*. Admin já pode fixar via `isAdmin` (tooling) independente disso.

---

## 🧭 Backlog competitivo restante — resoluções decision-ready (2026-06-08)

> **Disciplina (padrão-ouro = No-Invention + money-path-safety).** C7 e C8 eram os únicos blocos community-finish **seguros e de decisão-clara** → construídos e deployados. Os demais (C3/C4/C6/C9/C10/C11/C12) tocam **fluxo de dinheiro** (Stripe `line_items`/transfers/`paymentIntents`) **ou** carregam uma **decisão de produto/financeira não tomada**. Construí-los às cegas inventaria política de negócio que só o fundador decide — exatamente o que o Art. IV proíbe. Abaixo cada item vira "**uma decisão do fundador de distância de buildável**": a decisão pendente, a âncora de código onde engata, a recomendação e o risco. **Nada aqui foi shipado** — são prontos-para-construir, não construídos.

### C3 — order bump no checkout (decision-ready) — score 12

- **Decisão pendente (já é pergunta aberta #147 do backlog):** bump **único** (estilo Cakto) ou **múltiplos** bumps? E **qual** produto faz bump de qual (relação produto↔produto que hoje não existe).
- **Âncora de código:** `index.ts:1416` (`line_items` é item **único**); flag/entrada via `request.data` em `createCheckoutSession` (`index.ts:1300`). Engate é money-path (Stripe Checkout) → exige o gate de segurança LIVE da própria FASE.
- **Recomendação:** começar com **bump único** (1 produto adicional, 1-clique) — menor superfície, +25% de ticket documentado (Cakto). Modelar a relação como um campo opcional no curso (`bumpCourseId` + `bumpPriceMinor`) com **split idêntico** ao já auditado.
- **Risco:** money-path. Sem a decisão do split do bump (mesma taxa? taxa diferente?) e da relação de produto, qualquer build inventa regra financeira. **Não construir sem a decisão.**

### C4 — gating por nível (decision-ready) — score 12, "o dente da retenção"

- **Decisão pendente (a essência):** **o quê** gatear e em **qual nível**? Opções não-equivalentes: (a) gatear **criar post** (risco: barra aluno novo → mata onboarding); (b) gatear **DM/chat** (mata DM-spam — uso documentado do Skool); (c) gatear **seções premium de curso** ("Members of a certain level", vídeo t=01:17) como **recompensa**.
- **Âncora de código:** o motor (C1) já existe — `memberStats/{uid}.level` (`src/domain/gamification.ts`), lido no feed (`course-community-feed.tsx:100`). Um gate engataria no `allow create` de `communityPosts`/`comments` (regra) **e**/ou no render de lição (`enrolled-course-workspace.tsx`). Drip já tem precedente de unlock (`domain/drip-policy.ts`, `getLessonUnlockState`).
- **Recomendação (menor-regret):** **NÃO** gatear participação básica (postar/comentar) — isso fere engajamento de aluno novo. Gatear como **recompensa**: desbloquear um recurso social/cosmético ou uma trilha bônus ao subir de nível. Threshold sugerido conservador (ex.: nível 2-3 p/ um primeiro unlock), espelhando o ladder Skool.
- **Risco:** **decisão de produto pura.** Gatear o alvo errado regride UX silenciosamente. A escolha do alvo+nível é do fundador; o build em si (gate na regra/no render) é mecânico depois disso.

### C6 — programa de afiliados (decision-ready) — score 10

- **Decisão pendente:** **% de comissão** (Skool=40% recorrente), **recorrente vs one-shot**, **janela de atribuição**, e tratamento fiscal do 2º leg de payout.
- **Âncora de código:** `grep affiliate`=**0** (não existe). MAS ledger+transfer Stripe são reusáveis (`index.ts:2039-2280`) — falta **atribuição** (quem indicou) + **2º leg de payout** (pagar o afiliado).
- **Recomendação:** modelar `affiliateId` no checkout (param de atribuição) + um 2º `transfer` no fluxo de release já auditado, gated por flag. Reusar o ledger existente (não inventar contabilidade nova).
- **Risco:** **money-path duplo** (2º leg de transferência = dinheiro real saindo p/ terceiro). Comissão/atribuição são decisão financeira do fundador. **Não construir sem os números.**

### C9 — upsell/downsell pós-compra 1-clique (decision-ready) — score 6

- **Decisão pendente:** quais ofertas sequenciais, e aceitar a **mudança de primitivo de pagamento**.
- **Âncora de código (bloqueio real):** `paymentIntents.create`=**0 hits**; checkout usa `customer_email` only → **"not a persistent Customer"** (`index.ts:2946`). 1-clique pós-compra exige um **Customer persistente + PaymentIntent** salvo — primitivo que **não existe** hoje. Construção BE maior.
- **Recomendação:** pré-requisito = migrar checkout p/ criar `Customer` persistente (decisão arquitetural com efeito em todo o fluxo de cobrança). Só depois o upsell 1-clique é viável.
- **Risco:** money-path + mudança de primitivo. **Registro, não build** — mexe na fundação de cobrança.

### C10 — quizzes/assessment runtime (decision-ready) — score 6

- **Decisão pendente (produto):** modelo de **question/answer/grade** (tipos de questão, correção automática vs manual, peso, retry). Não existe.
- **Âncora de código:** `quiz` é `LessonType` selecionável no builder, mas o learner renderiza **placeholder** (`enrolled-course-workspace.tsx:722`). Já há nota de pré-condição em T3 (linha 486): quando `teacherStudio.courseBuilder` ligar, a opção quiz precisa ser **terminada ou escondida**.
- **Recomendação:** definir o modelo mínimo (múltipla-escolha auto-corrigida) **antes** de construir; ligar atrás do mesmo flag do course-builder. Não é money-path → menor risco depois da decisão de modelo.
- **Risco:** inventar o modelo de avaliação às cegas = product-decision. **Registro até o fundador definir o modelo.**

### C11 — checkout builder drag-drop (decision-ready) — score 3

- **Decisão pendente:** substituir/abandonar **Stripe Checkout hospedado** por um checkout próprio (escassez/prova-social/exit-popup/A-B, múltiplos checkouts por produto).
- **Âncora de código:** `createCheckoutSession` (`index.ts:1300`) = Stripe Checkout **hospedado fixo**, 1 checkout/curso, zero customização. C11 é uma **re-arquitetura** (checkout próprio = PCI/UX/segurança nova).
- **Recomendação:** **não** priorizar — score 3, esforço 5. Se buscado, é projeto dedicado com revisão de segurança de pagamento própria.
- **Risco:** money-path + superfície PCI. **Registro.**

### C12 — multi-preço / multi-oferta por produto (decision-ready) — score 6

- **Decisão pendente:** estrutura de **tabela de ofertas** (até 10 preços/ofertas por produto, estilo Cakto) e como o split/fee se aplica por oferta.
- **Âncora de código:** hoje **1 preço/curso** (`TeacherCourse.priceAmountMinor`); sem tabela de ofertas. Engata em `createCheckoutSession` (`index.ts:1300`) + no schema do curso.
- **Recomendação:** modelar `offers: Array<{id, priceMinor, label}>` com split derivado do mesmo `platformFeeBps` já auditado; checkout seleciona a oferta por id.
- **Risco:** money-path (cada oferta = preço cobrado real). **Registro até a estrutura de oferta ser decidida.**

---

## 🌑 Dark mode — polimento estético (registro de BUILD, 2026-06-08)

> **Contexto do pedido:** "aprimorar a versão dark da página que ainda tá estranha". Defeito-raiz mapeado e corrigido na fonte; verificação por **prova de bundle** (não-visual), porque toda superfície tematizada é auth-gated.

### Diagnóstico — por que estava "estranha"

O dark mode aplica via `<html data-theme="dark">` setado pelo `ThemeProvider` (`lib/theme/theme-provider.tsx`), que envolve **apenas** `components/platform/platform-shell.tsx` (linhas 49/127) — superfícies autenticadas. Tokens viram no bloco `[data-theme="dark"]` de `globals.css`. Havia um override global `[data-theme="dark"] .bg-white { background-color: var(--color-surface) !important }` que pega a classe `.bg-white` **exata**, mas **não** alcança seletores compostos (`hover:bg-white`, `focus:bg-white`), opacidade (`bg-white/NN`) nem hex arbitrário (`bg-[#fff]`). Resultado: **flash branco no hover** de linhas/itens e **barras translúcidas brancas** sobre superfície de tema → o "estranho".

### Correções aplicadas (rastreáveis)

| # | Fix | Arquivo(s) | Antes → Depois |
|---|-----|-----------|----------------|
| 1 | **Token de hover dedicado** (lift em vez de flash branco) | `src/app/globals.css` | `+ --color-surface-hover: #ffffff` (`:root`) e `+ --color-surface-hover: #1c2841` (`[data-theme="dark"]` = surface-strong, lift suave sobre surface `#0f1626`) |
| 2 | **10 migrações** `hover:/focus:bg-white` → `hover:/focus:bg-[var(--color-surface-hover)]` | account-panel.tsx (2×), plans-panel.tsx, community-leaderboard.tsx, course-community-feed.tsx (2×: 207/240), learn-dashboard.tsx, session-card.tsx, listing-search-bar.tsx, teacher-course-studio.tsx | flash branco → lift de tema |
| 3 | **Barra de matrícula translúcida** | `src/app/courses/[slug]/page.tsx:268` | `bg-white/95` + `bg-white/85` → `bg-[var(--color-surface)]/95` + `/85` |
| 4 | **Pill do course-builder** | `src/components/teacher/course-builder-studio.tsx:1366` | `bg-white/70` → `bg-[var(--color-surface)]/70` |

### Varreduras de garantia (zero-leak provado na fonte)

- `bg-white/NN` / `bg-[#fff]` / gradiente-white em `src`: só sobrou em `components/site/*` (**marketing — fora do `ThemeProvider`**, não tematizável) e chips frosted sobre `text-white` em contexto escuro (wallet-panel, hero badges, wishlist-heart sobre capa) → **corretos, não são leak**.
- `bg-(slate|gray|zinc|neutral|stone)-(50|100|200)` e hex claro arbitrário de fundo: **0 ocorrências** em `src`.
- `text-black` / `text-*-(800|900)` / hex escuro arbitrário de texto (risco dark-on-dark): **0 ocorrências** em `src`.
- Conclusão: o codebase é disciplinado em tokens CSS-var; a superfície de leak fica fechada exatamente aos casos 1-4 acima.

### Verificação (prova de bundle — visual impossível sem credenciais)

`/account` e demais rotas de plataforma renderizam o gate **"SIGN IN REQUIRED"** *fora* do `ThemeProvider` (curto-circuito antes do shell montar), então o preview local não exibe superfície tematizada sem login (não autentico — política). Prova feita no artefato compilado:

- `npm run build` → **compila limpo** (árvore de rotas completa, zero erro).
- `.next/static/chunks/*.css` contém **`--color-surface-hover:#fff`** (light) **e `--color-surface-hover:#1c2841`** (dark — valor que só existe no bloco `[data-theme="dark"]`, prova que a rampa dark está íntegra).
- `var(--color-surface-hover)` referenciado 2× → utilitários `hover:`/`focus:` gerados corretamente pelo Tailwind v4.

### ⚠️ Follow-up DEFERIDO (registro, não build)

- **Stripe embedded `appearance.colorBackground`:** componentes Connect/Elements embarcados têm `appearance` próprio; se `colorBackground` ficar branco fixo, o widget Stripe aparece branco no dark. Exige a Appearance API da Stripe **e** sessão Stripe viva p/ testar (não verificável no preview sem auth). Money-path-adjacent (UI de pagamento) → **não tocar sem poder verificar**; documentado p/ uma rodada com credenciais de teste Stripe.
