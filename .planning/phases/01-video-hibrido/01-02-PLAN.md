---
phase: 01-video-hibrido
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/learn/enrolled-course-workspace.tsx
  - src/components/courses/creator-course-detail.tsx
autonomous: true
requirements: [VID-05]

must_haves:
  truths:
    - "Lesson with videoSource 'youtube' plays the youtube-nocookie embed in the student classroom even when an orphaned hosted asset exists"
    - "Lesson with videoSource 'upload' plays the hosted video (BunnyVideoPlayer when bunnyVideoId, else ProtectedAssetPreview) even when an externalUrl exists"
    - "Legacy lesson without videoSource plays exactly what it played before (asset wins, else trusted embed) via inferLessonVideoSource"
    - "Creator public preview does not render an embed for a lesson whose videoSource is 'upload'"
  artifacts:
    - path: "src/components/learn/enrolled-course-workspace.tsx"
      provides: "LessonContentPanel branches on explicit videoSource instead of asset-presence precedence"
      contains: "inferLessonVideoSource"
    - path: "src/components/courses/creator-course-detail.tsx"
      provides: "public preview embed gated on videoSource"
      contains: "videoSource"
  key_links:
    - from: "src/components/learn/enrolled-course-workspace.tsx"
      to: "src/domain/teacher-course.ts"
      via: "lesson.videoSource ?? inferLessonVideoSource(...) resolution"
      pattern: "videoSource \\?\\? inferLessonVideoSource"
    - from: "LessonContentPanel youtube branch"
      to: "src/domain/lesson-embed.ts"
      via: "getTrustedLessonEmbed(lesson.externalUrl) — unchanged, still the single embed gate"
      pattern: "getTrustedLessonEmbed"
    - from: "LessonContentPanel upload branch"
      to: "BunnyVideoPlayer / ProtectedAssetPreview"
      via: "existing components, entered only when resolved source is upload"
      pattern: "BunnyVideoPlayer"
---

<objective>
Make the student player and the creator public preview respect `videoSource` explicitly, replacing the hardcoded `primaryHostedVideo > trustedEmbed` precedence (the latent bug where an orphaned asset silently beats the creator's YouTube URL).

Purpose: VID-05 — the player obeys the creator's explicit choice; legacy lessons keep behaving identically via inference.
Output: `LessonContentPanel` branch keyed on resolved source; `creator-course-detail.tsx` preview gated. Builder preview (`creator-course-workspace.tsx` / `course-preview-shell.tsx`) is covered automatically — it reuses this same panel and the mapper fixed in plan 01-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-video-hibrido/01-CONTEXT.md
@.planning/phases/01-video-hibrido/01-RESEARCH.md
@.planning/phases/01-video-hibrido/01-01-SUMMARY.md

@src/components/learn/enrolled-course-workspace.tsx
@src/components/courses/creator-course-detail.tsx

<interfaces>
From plan 01-01 (src/domain/teacher-course.ts):
```typescript
export function inferLessonVideoSource(params: {
  hasVideoAsset: boolean;
  hasTrustedEmbed: boolean;
}): "upload" | "youtube" | null;
// TeacherLesson.videoSource?: "youtube" | "upload" | null
```
From plan 01-01 (src/domain/learning.ts): `Lesson.videoSource?: "youtube" | "upload" | null` — already flowing into LessonContentPanel via resolvedSelectedLesson spread (no change needed there, RESEARCH item 13).

Current code to replace (enrolled-course-workspace.tsx L1279-1331):
```typescript
const primaryHostedVideo = locked ? null
  : assets.find((a) => (a.kind === "lesson_video" || a.kind === "live_recording") && a.contentType.startsWith("video/")) ?? null;
const trustedEmbed = locked || primaryHostedVideo ? null : getTrustedLessonEmbed(lesson.externalUrl);
// render: locked → empty | bunnyVideoId → BunnyVideoPlayer | asset → ProtectedAssetPreview | embed → iframe | empty
```
creator-course-detail.tsx (~L198-224): works directly on TeacherLesson (not mapped Lesson); uses getTrustedLessonEmbed for the free-preview lesson; NEVER fetches course_assets (pre-existing structural limit — RESEARCH Risk #2, not fixed in this phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded precedence in LessonContentPanel with explicit videoSource branch (VID-05)</name>
  <files>src/components/learn/enrolled-course-workspace.tsx</files>
  <action>
    In LessonContentPanel() (L1279-1331):
    1. Keep the `primaryHostedVideo` finder as-is (same predicate) but ALSO compute the trusted embed WITHOUT the `|| primaryHostedVideo` guard: `const trustedEmbed = locked ? null : getTrustedLessonEmbed(lesson.externalUrl);`.
    2. Resolve the source once: `const resolvedVideoSource = lesson.videoSource ?? inferLessonVideoSource({ hasVideoAsset: Boolean(primaryHostedVideo), hasTrustedEmbed: Boolean(trustedEmbed) });` (import inferLessonVideoSource from @/domain/teacher-course — adjust to the repo's absolute-import alias in use).
    3. Rewrite ONLY the branch selection in the render (locked check stays first and unchanged):
       - resolvedVideoSource === "upload": render BunnyVideoPlayer (when primaryHostedVideo?.bunnyVideoId) or ProtectedAssetPreview (when primaryHostedVideo) — exactly the existing JSX; if no asset exists, fall through to the existing empty state.
       - resolvedVideoSource === "youtube": render the existing trustedEmbed iframe; if the URL is absent/invalid, existing empty state.
       - null: existing empty states.
    4. Anti-pattern guard (RESEARCH): once videoSource is explicit, NEVER fall back to "there is an asset, show it anyway" — an explicit "youtube" lesson with an orphaned asset must render the embed.
    Do NOT touch BunnyVideoPlayer, ProtectedAssetPreview, the iframe attributes, /api/courses/video-token, or canViewCourseAssetVideo (VID-04 lock — entitlement gate untouchable).
    Commit: `feat(learn): player respects explicit lesson videoSource, refs #2`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Branch selection keyed on resolvedVideoSource; explicit youtube beats orphaned asset and vice versa; legacy lessons resolve via inference to the exact pre-change behavior; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Gate creator public preview embed on videoSource (VID-05 "vice versa")</name>
  <files>src/components/courses/creator-course-detail.tsx</files>
  <action>
    Around L198-224: this component reads TeacherLesson directly, so lesson.videoSource is available with zero mapping. Gate the previewLessonEmbed usage: only compute/render the embed when `(lesson.videoSource ?? "youtube-or-null-inference") !== "upload"` — concretely, since this component cannot know about assets (never fetches course_assets), the rule is: if `lesson.videoSource === "upload"`, do not render an embed (show whatever non-embed fallback the component already has); otherwise keep current behavior (embed when getTrustedLessonEmbed resolves). Do not attempt asset playback here — pre-existing structural limit, explicitly out of scope (RESEARCH Risk #2).
    Commit: `feat(courses): public preview respects lesson videoSource, refs #2`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>Upload-source lessons never render an embed in the public preview; youtube/legacy behavior unchanged; lint, typecheck, tests green.</done>
</task>

</tasks>

<verification>
- Full suite + typecheck + lint green.
- Grep: `inferLessonVideoSource` imported in enrolled-course-workspace.tsx; no remaining `locked || primaryHostedVideo` guard on the embed computation.
- BunnyVideoPlayer / video-token route / canViewCourseAssetVideo diff-clean (git diff shows no changes to those files).
</verification>

<success_criteria>
- VID-05: playback branch driven by explicit videoSource with inference fallback for legacy lessons; no silent asset-over-embed precedence anywhere (classroom, builder preview via shared panel, public preview).
</success_criteria>

<output>
After completion, create `.planning/phases/01-video-hibrido/01-02-SUMMARY.md`
</output>
