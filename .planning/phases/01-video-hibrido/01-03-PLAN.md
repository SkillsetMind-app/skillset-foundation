---
phase: 01-video-hibrido
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/teacher/lesson-video-source-picker.tsx
  - src/components/teacher/lesson-video-source-picker.test.tsx
  - src/components/teacher/lesson-content-modal.tsx
  - src/app/globals.css
autonomous: true
requirements: [VID-02, VID-03, VID-04, VID-07]

must_haves:
  truths:
    - "Video tab shows a two-option source picker (YouTube | Upload); only the chosen source's input is visible/active"
    - "Legacy lesson without videoSource shows its inferred source pre-selected in the picker"
    - "Switching source patches only videoSource — externalUrl is never cleared and assets are never deleted"
    - "Pasting a YouTube URL on a youtube-source lesson flows through the existing getTrustedLessonEmbed status (unchanged gate)"
    - "Picker follows DESIGN-CLONE-SPEC anatomy (44px cards / var(--radius-sm) 8px, 40px solid / var(--radius-xs) 6px, Ink Indigo + Brass, heavy headings) as a scoped island — global tokens untouched"
  artifacts:
    - path: "src/components/teacher/lesson-video-source-picker.tsx"
      provides: "dependency-free presentational picker component"
      exports: ["LessonVideoSourcePicker"]
      min_lines: 30
    - path: "src/components/teacher/lesson-video-source-picker.test.tsx"
      provides: "RTL tests: renders options, active state, onChange, disabled"
      contains: "LessonVideoSourcePicker"
    - path: "src/components/teacher/lesson-content-modal.tsx"
      provides: "Video tab gated by resolved source; picker wired to onUpdateLesson"
      contains: "videoSource"
    - path: "src/app/globals.css"
      provides: "scoped .lesson-video-source-picker CSS island + additive --color-ink-indigo/--color-brass tokens"
      contains: "--color-ink-indigo"
  key_links:
    - from: "src/components/teacher/lesson-video-source-picker.tsx"
      to: "src/components/teacher/course-builder-studio.tsx updateLesson()"
      via: "onChange → onUpdateLesson({ videoSource }) generic Partial<TeacherLesson> patch (no studio change needed)"
      pattern: "onUpdateLesson\\(\\{ videoSource"
    - from: "src/components/teacher/lesson-content-modal.tsx"
      to: "src/domain/teacher-course.ts"
      via: "resolvedSource = lesson.videoSource ?? inferLessonVideoSource(...)"
      pattern: "inferLessonVideoSource"
    - from: "youtube branch of the Video tab"
      to: "src/domain/lesson-embed.ts"
      via: "existing URL input + inline embed status (unchanged)"
      pattern: "getTrustedLessonEmbed"
---

<objective>
Build the Eduzz-style source picker in the lesson modal's Video tab: an explicit YouTube-vs-Upload choice where only the active source's input renders, styled per DESIGN-CLONE-SPEC as a scoped island, with the picker extracted as a dependency-free component so it gets a lightweight RTL test (no Supabase mocking).

Purpose: VID-02 (picker UX), VID-03 (keep getTrustedLessonEmbed as the only embed gate, now wired to the choice), VID-04 (upload UI untouched behind the gate), VID-07 (spec anatomy, no global reskin — that is Phase 3).
Output: new picker component + test, wired modal, CSS island.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-video-hibrido/01-CONTEXT.md
@.planning/phases/01-video-hibrido/01-RESEARCH.md
@.planning/phases/01-video-hibrido/01-01-SUMMARY.md

@src/components/teacher/lesson-content-modal.tsx
@src/components/learn/members-area-hero.test.tsx

<interfaces>
From plan 01-01 (src/domain/teacher-course.ts):
```typescript
export function inferLessonVideoSource(params: {
  hasVideoAsset: boolean;
  hasTrustedEmbed: boolean;
}): "upload" | "youtube" | null;
// TeacherLesson.videoSource?: "youtube" | "upload" | null
```
Already available inside lesson-content-modal.tsx (RESEARCH, verified): `videoAssets` (L130-132), `trustedEmbed` (L135), `onUpdateLesson(patch: Partial<TeacherLesson>)` (generic merge in course-builder-studio.tsx L1115-1137 — zero studio changes needed).
Video tab structure (L353-439): status banner → 2-up upload-choice buttons → LessonUploadForm → YouTube/Vimeo URL field → inline embed status → LessonAssetList. Today ALL are unconditionally rendered.
Design tokens that already exist in globals.css: `--radius-xs: 6px` (L61), `--radius-sm: 8px` (L57). Ink Indigo `#14182B` / Brass `#C6A15B` do NOT exist as tokens — introduce additively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract LessonVideoSourcePicker (presentational) + RTL test (VID-02)</name>
  <files>src/components/teacher/lesson-video-source-picker.tsx, src/components/teacher/lesson-video-source-picker.test.tsx</files>
  <behavior>
    - Renders exactly two options: "YouTube link" and "Video upload" (EN copy, consultive tone — product is EN-first)
    - value="youtube" → YouTube option carries the active class/aria-pressed, Upload does not (and vice versa)
    - value=null → neither option active
    - Clicking an option calls onChange with "youtube" | "upload"; clicking the already-active option does not need to fire (either behavior acceptable, assert whichever is implemented)
    - disabled=true → both buttons disabled, onChange never fires
  </behavior>
  <action>
    Create src/components/teacher/lesson-video-source-picker.tsx: a dependency-free presentational component (NO Supabase imports, NO data hooks — this is what makes it testable like members-area-hero.test.tsx):
    ```typescript
    export function LessonVideoSourcePicker(props: {
      value: "youtube" | "upload" | null;
      disabled?: boolean;
      onChange: (next: "youtube" | "upload") => void;
    })
    ```
    Two radio-card buttons (role/semantics: button + aria-pressed, matching the existing .lesson-modal-choice interaction pattern — reuse the PATTERN, not its CSS). Lucide icons already imported in the modal file family: use Link2 for YouTube, UploadCloud for upload. Root className `lesson-video-source-picker`, option className with `is-active` modifier.
    Write the test file FIRST (vitest + @testing-library/react, mirror members-area-hero.test.tsx setup), watch it fail, then implement.
    Commit: `feat(teacher): add LessonVideoSourcePicker component with tests, refs #2`
  </action>
  <verify>
    <automated>npx vitest run src/components/teacher/lesson-video-source-picker.test.tsx</automated>
  </verify>
  <done>Component renders/toggles/disables per behavior list; test file green with zero mocking of Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Scoped CSS island per DESIGN-CLONE-SPEC (VID-07)</name>
  <files>src/app/globals.css</files>
  <action>
    1. In `:root`, ADD (purely additive — nothing reads them yet, so zero blast radius): `--color-ink-indigo: #14182B;` and `--color-brass: #C6A15B;`.
    2. Add a `.lesson-video-source-picker` block near the existing `.lesson-modal*` styles (~L2965-3560):
       - Option cards: min-height 44px, `border-radius: var(--radius-sm)` (8px), font-weight 400 idle / 700 on `.is-active`, active border/accent in `var(--color-brass)`, active text/surface accents from `var(--color-ink-indigo)`.
       - Any solid/primary button inside the island: height 40px, `border-radius: var(--radius-xs)` (6px), font-weight 400.
       - Picker heading/label: font-weight 600-700 ("headings pesados").
       - Use the tokens via var(), never hardcoded 6px/8px literals.
    3. Do NOT modify `--color-primary`, `--color-accent`, `.lesson-modal-choice` (14px radius, crimson — old skin, left alone), or `.lesson-modal-video` banner. Global Cosmos→SkillsetMind reskin is Phase 3 (RESEARCH scope boundary) — this island is the entire VID-07 surface.
    Commit: `feat(design): scoped source-picker island per DESIGN-CLONE-SPEC, refs #2`
  </action>
  <verify>
    <automated>npm run lint && grep -c "color-ink-indigo" src/app/globals.css</automated>
  </verify>
  <done>Island classes exist using var(--radius-xs)/var(--radius-sm) and the two new additive tokens; git diff on globals.css shows no edits to existing token values or existing class blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Wire picker into the Video tab — gate inputs by resolved source (VID-02, VID-03, VID-04)</name>
  <files>src/components/teacher/lesson-content-modal.tsx</files>
  <action>
    In the `tab === "video"` block (L353-439):
    1. Compute `const resolvedSource = lesson.videoSource ?? inferLessonVideoSource({ hasVideoAsset: videoAssets.length > 0, hasTrustedEmbed: Boolean(trustedEmbed) });` (import from the domain).
    2. Render `<LessonVideoSourcePicker value={resolvedSource} disabled={!isEditable} onChange={(next) => onUpdateLesson({ videoSource: next })} />` directly under the lesson-modal-video banner (L355-365), above everything else.
    3. Gate the existing blocks by visibility ONLY (conditional render, no data mutation):
       - resolvedSource === "upload": show the existing upload-choice buttons + LessonUploadForm + LessonAssetList; HIDE the URL field + inline embed status.
       - resolvedSource === "youtube": show the existing URL input + inline-status (getTrustedLessonEmbed keeps working untouched — VID-03); HIDE the upload UI.
       - resolvedSource === null (new lesson, no media): show only the picker with a short EN helper line (e.g. "Choose how this lesson delivers video"); both input blocks hidden until a source is picked.
    4. Guarantee non-destruction (VID-06/success criterion 5): switching source must ONLY call onUpdateLesson({ videoSource }) — never clear externalUrl, never delete/detach assets. Nothing in this component currently does either on visibility change, so hiding (not resetting) is sufficient — keep it that way.
    5. Leave getAssetStatusLabel() (tab badge, L82-96) inference-based — smallest diff, Claude's-discretion call per RESEARCH Open Question #2. Leave live_recording upload choice inside the upload branch as-is (fonte "upload" covers it; Vimeo stays accepted under the "YouTube link" label — product label, not a tech restriction).
    Commit: `feat(teacher): Eduzz-style video source picker in lesson modal, refs #2`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>Only the active source's input renders; picker pre-selects the inferred source for legacy lessons; switching sources round-trips through onUpdateLesson and preserves both sources' data; full suite green.</done>
</task>

</tasks>

<verification>
- `npm test` green including the new picker test file.
- `npx tsc --noEmit` + `npm run lint` clean.
- Grep: no new imports of getSupabaseBrowserClient in lesson-video-source-picker.tsx (must stay presentational).
- git diff on globals.css: additive only (new tokens + new island block).
- Upload pipeline files (course-assets.ts, video-token route) diff-clean — VID-04.
</verification>

<success_criteria>
- VID-02: explicit two-way picker, only the chosen source's input visible, non-destructive switching.
- VID-03: getTrustedLessonEmbed remains the single embed gate, now reached only through the youtube branch.
- VID-04: zero changes to Bunny TUS / token / entitlement / Supabase Storage fallback.
- VID-07: picker matches spec anatomy via a scoped island; no global token repointing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-video-hibrido/01-03-SUMMARY.md`
</output>
