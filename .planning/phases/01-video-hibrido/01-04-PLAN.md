---
phase: 01-video-hibrido
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: []
autonomous: false
requirements: [VID-04, VID-05, VID-07]

must_haves:
  truths:
    - "Creator sees the source picker in the Video tab and only the chosen source's input is active"
    - "YouTube URL (watch/youtu.be/shorts/live) on a youtube-source lesson plays as youtube-nocookie in the student classroom"
    - "Upload-source lesson plays the hosted video exactly as before this phase"
    - "Legacy lesson without videoSource keeps playing what it played, and the modal shows the inferred source"
    - "Switching a lesson's source preserves the other source's data and the player follows the new choice"
  artifacts: []
  key_links: []
---

<objective>
Final gate for the phase: run the full automated suite, then a single human verification pass over the five ROADMAP success criteria plus the VID-07 anatomy table (visual anatomy is manual-only — no visual-regression tooling exists in this repo, per RESEARCH Validation Architecture).

Purpose: confirm the end-to-end flow (builder → save → classroom) with human eyes before the phase is declared done and the PR for issue #2 goes up.
Output: verified phase, ready for PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-video-hibrido/01-02-SUMMARY.md
@.planning/phases/01-video-hibrido/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated verification gate</name>
  <files></files>
  <action>
    From the repo root, run the complete gate: lint, typecheck, full vitest suite. All three must pass with zero errors. If anything fails, fix forward inside this plan (small fixes) or report the gap — do not proceed to the checkpoint with a red gate.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Lint, typecheck and full test suite green on feat/issue-2-hybrid-video.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human pass — 5 success criteria + VID-07 anatomy</name>
  <files>none (verification only)</files>
  <action>Pause and hand off to the user for manual verification of the running app against the 5 ROADMAP success criteria and the VID-07 anatomy table (steps in how-to-verify below). Do not modify code in this task.</action>
  <verify>User walks the 5 numbered steps in how-to-verify and replies "approved" or describes the failing step.</verify>
  <done>User approval received on all 5 steps; any reported issue routed to gap closure.</done>
  <what-built>Explicit per-lesson videoSource with Eduzz-style source picker: domain field + inference (01-01), player/preview respecting the choice (01-02), picker UI + scoped design island (01-03).</what-built>
  <how-to-verify>
    Run `npm run dev` and test as a teacher account with at least one course:
    1. PICKER (VID-02/07): Open a lesson → Video tab. Confirm the two-option picker (YouTube link | Video upload) renders above the inputs; only the selected source's input is visible. Anatomy check: option cards ~44px tall with 8px radius, active state uses Brass (#C6A15B) accent on Ink Indigo, headings noticeably heavy, rest of the app shell still the old crimson skin (no global reskin leaked).
    2. YOUTUBE FLOW (criterion 2): Select YouTube, paste a `youtube.com/watch?v=...` URL (also try a `youtu.be/` short link), save, open the lesson as an enrolled student → the video must render inside a `youtube-nocookie.com/embed/` iframe (check via devtools element inspector).
    3. UPLOAD FLOW (criterion 3, VID-04): Select Upload on a lesson that has a hosted video → plays exactly as before (Bunny player if configured, signed Supabase preview otherwise).
    4. LEGACY (criterion 4): Open a pre-existing lesson never touched by this phase → it plays what it played before, and its modal shows the inferred source pre-selected.
    5. SWITCHING (criterion 5): On a lesson that has BOTH a YouTube URL and an uploaded asset: set source=youtube → student sees the embed (asset ignored); switch to upload → student sees the hosted video (URL ignored but still present in the modal when switching back). Confirm neither switch deleted the URL or the asset.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase (next: PR for issue #2), or describe which step failed.</resume-signal>
</task>

</tasks>

<verification>
- Automated gate green (Task 1).
- Human approval on all 5 steps (Task 2).
</verification>

<success_criteria>
All five ROADMAP Phase 1 success criteria confirmed true by a human on the running app, plus VID-07 anatomy confirmed within the scoped island only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-video-hibrido/01-04-SUMMARY.md`
</output>
