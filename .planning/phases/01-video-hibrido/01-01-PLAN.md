---
phase: 01-video-hibrido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/teacher-course.ts
  - src/domain/teacher-course.test.tsx
  - src/domain/learning.ts
  - src/lib/data/published-courses.ts
  - docs/technical/data-model.md
autonomous: true
requirements: [VID-01, VID-06]

must_haves:
  truths:
    - "A lesson saved with videoSource keeps it after normalizeTeacherCourseModules (survives into the RPC payload — no field stripping)"
    - "inferLessonVideoSource returns 'upload' when a video asset exists, 'youtube' when only a trusted embed exists, null otherwise"
    - "Student-facing Lesson objects carry videoSource through teacherCourseToLearningCourse"
  artifacts:
    - path: "src/domain/teacher-course.ts"
      provides: "TeacherLesson.videoSource field, normalizeVideoSource handling inside normalizeTeacherCourseModules, exported inferLessonVideoSource()"
      contains: "inferLessonVideoSource"
    - path: "src/domain/learning.ts"
      provides: "Lesson.videoSource field"
      contains: "videoSource"
    - path: "src/lib/data/published-courses.ts"
      provides: "videoSource in the per-lesson allowlist of teacherCourseToLearningCourse"
      contains: "videoSource"
    - path: "src/domain/teacher-course.test.tsx"
      provides: "updated exact-equality fixture + new test cases for normalize pass-through and inference"
      contains: "inferLessonVideoSource"
  key_links:
    - from: "src/domain/teacher-course.ts"
      to: "src/lib/data/teacher-courses.ts updateTeacherCourseBuilder p_payload"
      via: "normalizeTeacherCourseModules output (RPC is blind JSONB pass-through per RESEARCH addendum)"
      pattern: "videoSource"
    - from: "src/lib/data/published-courses.ts"
      to: "src/domain/learning.ts Lesson"
      via: "teacherCourseToLearningCourse per-lesson mapping"
      pattern: "videoSource: lesson\\.videoSource"
---

<objective>
Add the explicit `videoSource: 'youtube' | 'upload'` field to the lesson domain model with legacy inference, and thread it through BOTH hand-maintained field allowlists (write-path normalize + read-path mapper) so it persists to Postgres and reaches the student-facing `Lesson` type.

Purpose: this is the foundation every other plan in the phase builds on (VID-01, VID-06). Per RESEARCH addendum, the `update_teacher_course_builder` RPC passes `modules` JSONB through blind — NO migration and NO RPC change. The only survival points are the two client-side choke points edited here.
Output: domain types + inference function + both mappers updated, all domain tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-video-hibrido/01-CONTEXT.md
@.planning/phases/01-video-hibrido/01-RESEARCH.md

Key sources (line numbers verified in RESEARCH):
@src/domain/teacher-course.ts
@src/domain/teacher-course.test.tsx
@src/domain/learning.ts
@src/lib/data/published-courses.ts

<interfaces>
Current TeacherLesson (src/domain/teacher-course.ts ~L60-70): id, title, type, description, durationMinutes, contentText, externalUrl, dripDelayDays, thumbnailAssetId.
Current student Lesson (src/domain/learning.ts ~L22-32): id, title, type, duration, isPreview, description, contentText, externalUrl, dripDelayDays.
normalizeTeacherCourseModules(modules: TeacherCourseModule[]): TeacherCourseModule[] — L265-287, per-lesson map is the single write-path choke point (used by both the RPC payload in teacher-courses.ts AND the builder change-signature via buildBuilderDraftPayload).
teacherCourseToLearningCourse() lesson mapping (published-courses.ts ~L326-338) is an explicit per-field allowlist — fields not listed are DROPPED (thumbnailAssetId proves it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add videoSource to domain write path + inference function (VID-01, VID-06)</name>
  <files>src/domain/teacher-course.ts, src/domain/teacher-course.test.tsx</files>
  <behavior>
    - inferLessonVideoSource({hasVideoAsset: true, hasTrustedEmbed: true}) === "upload" (asset wins)
    - inferLessonVideoSource({hasVideoAsset: false, hasTrustedEmbed: true}) === "youtube"
    - inferLessonVideoSource({hasVideoAsset: false, hasTrustedEmbed: false}) === null
    - normalizeTeacherCourseModules preserves videoSource "youtube" and "upload" verbatim
    - normalizeTeacherCourseModules coerces absent/invalid videoSource values to null (never undefined-drops, never invents a source)
    - Existing exact .toEqual() test on normalizeTeacherCourseModules output (L110-154) updated in the SAME commit — it WILL break otherwise (guaranteed regression per RESEARCH item 3)
  </behavior>
  <action>
    In src/domain/teacher-course.ts:
    1. Add `videoSource?: "youtube" | "upload" | null;` to the TeacherLesson type (L60-70). Export a `LessonVideoSource = "youtube" | "upload"` type alias if it keeps call sites clean.
    2. Add a small `normalizeVideoSource(value: unknown): "youtube" | "upload" | null` internal helper (returns the value only if it is exactly "youtube" or "upload", else null) and call it inside the lessons.map block of normalizeTeacherCourseModules (L265-287): `videoSource: normalizeVideoSource(lesson.videoSource)`. This single edit makes save payload and change-signature symmetric automatically (Pattern 1 in RESEARCH).
    3. Add exported pure function per the locked VID-06 contract (RESEARCH "Inference function contract"):
       `export function inferLessonVideoSource(params: { hasVideoAsset: boolean; hasTrustedEmbed: boolean }): "upload" | "youtube" | null` — asset → "upload", else embed → "youtube", else null.
    In src/domain/teacher-course.test.tsx: update the existing fixture + expected output to include videoSource, then add the new cases from <behavior>. Write tests first, watch them fail, implement, watch them pass.
    Do NOT touch getTrustedLessonEmbed / lesson-embed.ts (VID-03 lock). Do NOT add videoSource to course_lesson_content or lesson-content.ts (routing flag, not gated content — RESEARCH "does NOT need to go").
    Commit: `feat(domain): add explicit lesson videoSource with legacy inference, refs #2`
  </action>
  <verify>
    <automated>npx vitest run src/domain/teacher-course.test.tsx</automated>
  </verify>
  <done>TeacherLesson has videoSource; normalizeTeacherCourseModules round-trips it; inferLessonVideoSource exported and covered by tests; full domain test file green including the updated exact-equality case.</done>
</task>

<task type="auto">
  <name>Task 2: Thread videoSource through the read-path allowlist (VID-01)</name>
  <files>src/domain/learning.ts, src/lib/data/published-courses.ts, docs/technical/data-model.md</files>
  <action>
    1. src/domain/learning.ts: add `videoSource?: "youtube" | "upload" | null;` to the student-facing Lesson type (~L22-32). Import/reuse the union from teacher-course.ts only if it does not create an awkward domain cross-import — a repeated literal union is acceptable here (both files are domain layer).
    2. src/lib/data/published-courses.ts, teacherCourseToLearningCourse() per-lesson map (~L326-338): add `videoSource: lesson.videoSource ?? null,` to the explicit allowlist. This is the SECOND hand-maintained field list (Pitfall 1 in RESEARCH) — without this line the field saves correctly but the player never sees it. rowToTeacherCourse needs NO change (opaque JSONB cast).
    3. docs/technical/data-model.md (~L83-96): add a videoSource row to the Lesson field table (one line, keeps the doc from going stale).
    Note: course-builder-studio.tsx needs NO change — updateLesson() is a generic Partial<TeacherLesson> merge, and new-lesson creation deliberately OMITS videoSource (falls to inference, smaller diff — RESEARCH item 5).
    Commit: `feat(data): carry lesson videoSource into student-facing Lesson mapping, refs #2`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>Lesson type exposes videoSource; teacherCourseToLearningCourse copies it; typecheck, lint and full test suite green.</done>
</task>

</tasks>

<verification>
- `npm test` fully green (including the updated exact .toEqual() case in teacher-course.test.tsx).
- `npx tsc --noEmit` clean.
- grep confirms `videoSource` present in BOTH choke points: normalizeTeacherCourseModules (teacher-course.ts) and the lesson map of teacherCourseToLearningCourse (published-courses.ts).
- No changes to: lesson-embed.ts, course-assets.ts, lesson-content.ts, supabase/migrations/ (RPC is pass-through — locked).
</verification>

<success_criteria>
- VID-01: videoSource persists with the course (both allowlists edited; RPC pass-through proven — no server change).
- VID-06: inferLessonVideoSource implements asset→upload, else embed→youtube, else null; pure, exported, tested.
- Zero regressions: existing domain tests updated and green in the same commits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-video-hibrido/01-01-SUMMARY.md`
</output>
